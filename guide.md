# 빌드 및 실행 가이드

## 요구사항

| 항목 | 내용 |
|------|------|
| OS | Windows 10 이상 |
| 컴파일러 | MinGW-w64 (gcc) |
| 표준 | C11 |

MinGW-w64 설치 확인:
```
gcc --version
```

---

## 빌드

프로젝트 루트(`chat_program/`의 상위 디렉토리)에서 실행한다.

```bash
# 서버 + 클라이언트 모두 빌드
make

# 서버만 빌드
make server

# 클라이언트만 빌드
make client
```

빌드 성공 시 루트에 `server.exe`, `client.exe` 생성된다.

---

## 초기 데이터 파일 생성 (최초 1회)

```bash
make init
```

`data/` 폴더 아래 9개의 빈 txt 파일이 생성된다. 서버 실행 전 반드시 수행해야 한다.

---

## 실행

터미널을 두 개 열고 각각 실행한다.

**터미널 1 — 서버:**
```bash
./server.exe
# 또는
server.exe
```

**터미널 2 — 클라이언트 (여러 개 실행 가능):**
```bash
./client.exe
# 또는
client.exe
```

기본 접속 주소: `127.0.0.1:55555`

---

## 정리

```bash
# 빌드 결과물 삭제 (data/ 파일은 유지)
make clean
```

---

## 주의사항

- `data/` 폴더가 없으면 서버가 새 레코드를 저장하지 못한다. `make init`으로 먼저 초기화할 것.
- 서버를 먼저 실행한 후 클라이언트를 실행해야 한다.
- 방화벽에서 TCP 55555 포트가 허용되어 있어야 한다.
