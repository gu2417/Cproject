# C응용프로젝트 요구사항 분석서

| 항목 | 내용 |
| --- | --- |
| 프로젝트명 | 대학 교직원을 위한 온라인 학생관리 시스템 |
| 문서명 | 요구사항 분석서 |
| 버전 | Version 1.1 |
| 작성일자 | 2026.04.27 |
| 작성자 |  |
| 기반 자료 | `reference/2026학년도_1학기_중간고사대체과제.pdf`, `requirements.md` |

## 1. 요구사항 리스트

### 1.1 서버 요구사항

| 순번 | 요구사항 ID | 요구사항명 | 변경일자 |
| --- | --- | --- | --- |
| 1 | S001 | 서버 실행 초기화 | 2026.04.27 |
| 2 | S002 | 학생 정보 파일 로딩 | 2026.04.27 |
| 3 | S003 | 사용자 정보 파일 로딩 | 2026.04.27 |
| 4 | S004 | 로그인 인증 처리 | 2026.04.27 |
| 5 | S005 | 인증 상태 및 권한 검증 | 2026.04.27 |
| 6 | S006 | 사용자 계정 추가 | 2026.04.27 |
| 7 | S007 | 사용자 계정 삭제 | 2026.04.27 |
| 8 | S008 | 학생 단일 조회 | 2026.04.27 |
| 9 | S009 | 학생 정보 추가 | 2026.04.27 |
| 10 | S010 | 학생 정보 수정 | 2026.04.27 |
| 11 | S011 | 학생 정보 삭제 | 2026.04.27 |
| 12 | S012 | 학과명 기반 학생 목록 조회 | 2026.04.27 |
| 13 | S013 | 단과대학 기반 학생 목록 조회 | 2026.04.27 |
| 14 | S014 | 학생 정보 파일 저장 | 2026.04.27 |
| 15 | S015 | 서버-클라이언트 메시지 검증 및 처리 | 2026.04.27 |
| 16 | S016 | 연결 리스트 및 메모리 관리 | 2026.04.27 |

### 1.2 클라이언트 요구사항

| 순번 | 요구사항 ID | 요구사항명 | 변경일자 |
| --- | --- | --- | --- |
| 1 | C001 | 로그인 전 화면 및 최초 사용자 등록 화면 | 2026.04.27 |
| 2 | C002 | 로그인 후 메인 메뉴 | 2026.04.27 |
| 3 | C003 | 학생 조회 화면 | 2026.04.27 |
| 4 | C004 | 학생 추가 화면 | 2026.04.27 |
| 5 | C005 | 학생 수정 화면 | 2026.04.27 |
| 6 | C006 | 학생 삭제 화면 | 2026.04.27 |
| 7 | C007 | 학과 및 단과대학 조회 화면 | 2026.04.27 |
| 8 | C008 | 사용자 계정 관리 화면 | 2026.04.27 |
| 9 | C009 | 서버 응답 출력 및 오류 표시 | 2026.04.27 |
| 10 | C010 | 입력값 검증 및 재입력 흐름 | 2026.04.27 |

### 1.3 기타 요구사항

| 순번 | 요구사항 ID | 요구사항명 | 변경일자 |
| --- | --- | --- | --- |
| 1 | ETC001 | 개발 환경 및 구현 제약 | 2026.04.27 |
| 2 | ETC002 | 학생 데이터 정의 | 2026.04.27 |
| 3 | ETC003 | 사용자 데이터 정의 | 2026.04.27 |
| 4 | ETC004 | 단과대학 데이터 정의 | 2026.04.27 |
| 5 | ETC005 | 파일 형식 및 구분자 규칙 | 2026.04.27 |
| 6 | ETC006 | 보안 및 권한 정책 | 2026.04.27 |
| 7 | ETC007 | 오류 코드 및 예외 처리 정책 | 2026.04.27 |
| 8 | ETC008 | 구조체 정의 | 2026.04.27 |
| 9 | ETC009 | 인수 기준 및 테스트 기준 | 2026.04.27 |
| 10 | ETC010 | 산출물 및 문서화 요구사항 | 2026.04.27 |

## 2. 상세 요구사항 분석

## 2.1 서버

### S001. 서버 실행 초기화

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S001 |
| 요구사항 명 | 서버 실행 초기화 |
| 상세 내용 | - 서버 프로그램은 클라이언트보다 먼저 실행되어 학생 관리 서비스를 제공할 준비를 완료해야 한다.<br>- 서버는 시작 시 환경 값, 데이터 파일 경로, 학생 리스트 헤드 포인터, 사용자 리스트 또는 사용자 파일 접근 상태, 단과대학 목록, 변경 여부 플래그를 초기화한다.<br>- 서버는 `SMU_Students.txt`와 `users.txt`의 사용 가능 여부를 확인한다.<br>- 서버는 학생 정보 로딩, 사용자 정보 확인, 단과대학 정보 초기화가 끝난 뒤 클라이언트 요청을 수신 가능한 상태로 전환한다.<br>- 서버는 실행 중 처리 가능한 명령 목록을 내부적으로 등록한다. 명령 목록에는 `LOGIN`, `FIRST_USER_REGISTER`, `USER_ADD`, `USER_DELETE`, `STUDENT_GET`, `STUDENT_ADD`, `STUDENT_UPDATE`, `STUDENT_DELETE`, `DEPARTMENT_LIST`, `COLLEGE_LIST`, `SAVE`, `LOGOUT`, `EXIT`이 포함된다.<br>- 초기화 완료 후 서버는 로딩된 학생 수, 사용자 수, 오류 레코드 수, 저장 파일 경로를 콘솔에 요약 출력한다. |
| 이슈사항 | - 서버 시작 순서가 보장되지 않으면 클라이언트가 연결 실패를 경험할 수 있다.<br>- 데이터 파일이 없거나 읽기 불가능하면 서비스 제공 범위가 달라진다.<br>- 초기화 도중 일부 단계만 성공하면 서버 상태가 불완전해질 수 있다. |
| 해결방안<br>(변경내역) | - 초기화 단계를 함수 단위로 분리하고 각 단계의 성공 여부를 명확히 반환한다.<br>- 필수 초기화 실패 시 서버는 요청 수신 상태로 진입하지 않고 오류를 출력한다.<br>- 학생 파일은 필수 파일로 보고, 없으면 실행 중단을 기본 정책으로 한다. 단, 개발 시험용으로 빈 리스트 시작 정책을 별도 옵션으로 둘 수 있다.<br>- 사용자 파일이 없거나 유효 사용자가 0명인 경우에는 최초 사용자 등록만 허용하는 제한 상태로 진입한다.<br>- 변경내역: Version 1.1에서 서버 초기화 조건, 명령 목록, 제한 상태를 구체화함. |

### S002. 학생 정보 파일 로딩

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S002 |
| 요구사항 명 | 학생 정보 파일 로딩 |
| 상세 내용 | - 서버는 시작 시 `SMU_Students.txt` 파일을 읽어 학생 정보를 연결 리스트에 적재한다.<br>- 파일의 한 줄은 학생 1명의 레코드로 처리한다.<br>- 학생 레코드의 기준 필드는 `학번`, `이름`, `소속학과`, `학년`, `GPA`, `이메일`, `모바일폰`이다.<br>- 기준 파일 형식은 `학번<TAB>이름<TAB>소속학과<TAB>학년<TAB>GPA<TAB>이메일<TAB>모바일폰`이다.<br>- 서버는 각 줄을 탭 문자 기준으로 분리하고 정확히 7개 필드를 확보해야 한다.<br>- 학번은 학생의 고유 식별자로 사용한다. 같은 학번이 2개 이상 발견되면 첫 번째 정상 레코드만 적재하고 이후 중복 레코드는 오류로 분류한다.<br>- 학년은 숫자로 변환 가능해야 하며 기본 허용 범위는 1~4이다. 단, 편입, 초과학기, 대학원 확장 가능성을 고려해야 하는 경우에는 구현 정책에 따라 허용 범위를 1~6으로 확장할 수 있다.<br>- GPA는 실수로 변환 가능해야 하며 기본 허용 범위는 0.0~4.5이다.<br>- 이메일은 빈 값이 아니어야 하며 최소한 `@`와 `.`을 포함해야 한다.<br>- 모바일폰은 빈 값이 아니어야 하며 숫자와 하이픈을 중심으로 구성되어야 한다.<br>- 정상 레코드는 `Student` 노드로 생성한 뒤 연결 리스트에 삽입한다.<br>- 서버는 약 9,000명의 학생 레코드를 로딩할 수 있어야 한다. |
| 이슈사항 | - 한글 파일을 Windows 콘솔에서 읽을 때 문자 인코딩 문제가 발생할 수 있다.<br>- 학생 파일에 빈 줄, 필드 수 불일치, 잘못된 GPA, 잘못된 학년, 중복 학번이 포함될 수 있다.<br>- 파일 끝 개행 유무에 따라 마지막 레코드가 잘못 처리될 수 있다.<br>- 모든 오류 레코드를 즉시 중단 처리하면 일부 정상 데이터도 사용할 수 없게 된다. |
| 해결방안<br>(변경내역) | - 파일은 텍스트 모드로 읽되, Windows 환경의 한글 처리를 고려하여 콘솔 코드 페이지 또는 파일 인코딩 정책을 개발계획서에 명시한다.<br>- 한 줄 파싱 함수, 필드 검증 함수, 노드 생성 함수를 분리한다.<br>- 빈 줄은 무시하고, 필드 수 불일치나 검증 실패는 오류 카운트에 포함한다.<br>- 중복 학번은 `ERR_DUPLICATE_STUDENT_ID`로 분류한다.<br>- 로딩 완료 후 정상 적재 건수와 오류 건수를 출력한다.<br>- 변경내역: Version 1.1에서 필드 검증 기준과 중복 처리 기준을 상세화함. |

### S003. 사용자 정보 파일 로딩

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S003 |
| 요구사항 명 | 사용자 정보 파일 로딩 |
| 상세 내용 | - 서버는 로그인과 계정 관리를 위해 `users.txt` 파일을 읽거나 요청 시 파일을 조회할 수 있어야 한다.<br>- 사용자 파일의 한 줄은 사용자 1명의 레코드로 처리한다.<br>- 사용자 레코드는 `사용자ID<TAB>사용자암호<TAB>사용자소속부서<TAB>사용자이름` 순서로 저장한다.<br>- 한 사용자 레코드는 4개 필드와 3개의 탭 구분자로 구성되어야 한다.<br>- 사용자 ID는 빈 값이 아니어야 하며 중복될 수 없다.<br>- 사용자 암호, 소속부서, 이름은 빈 값일 수 없다.<br>- 서버는 유효 사용자 수가 0명인지 판단할 수 있어야 한다.<br>- 유효 사용자 수가 0명이면 최초 사용자 등록 기능을 허용한다.<br>- 사용자가 1명 이상 존재하면 최초 사용자 등록은 차단하고 로그인 후 사용자 추가 기능만 허용한다. |
| 이슈사항 | - `users.txt`가 없으면 최초 관리자 생성 흐름이 필요하다.<br>- 중복 사용자 ID가 있으면 로그인 결과가 불명확해진다.<br>- 사용자 파일 저장 중 오류가 나면 계정 정보가 손상될 수 있다.<br>- 암호가 평문으로 저장되므로 노출 위험이 있다. |
| 해결방안<br>(변경내역) | - 사용자 파일이 없거나 크기가 0이면 최초 사용자 등록 가능 상태로 판단한다.<br>- 사용자 파일 로딩 또는 조회 시 중복 ID를 검출하고 오류를 출력한다.<br>- 사용자 추가 및 삭제는 임시 파일 기록 후 원본 교체 방식으로 저장한다.<br>- 과제 범위에서는 파일 기반 암호를 사용하되, 암호는 화면과 로그에 출력하지 않는다.<br>- 변경내역: Version 1.1에서 최초 사용자 등록 가능 조건과 사용자 파일 검증 기준을 추가함. |

### S004. 로그인 인증 처리

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S004 |
| 요구사항 명 | 로그인 인증 처리 |
| 상세 내용 | - 서버는 클라이언트가 전송한 사용자 ID와 암호를 기준으로 로그인 요청을 처리한다.<br>- 로그인 요청에는 사용자 ID와 암호가 반드시 포함되어야 한다.<br>- 서버는 `users.txt`의 사용자 ID와 사용자 암호가 모두 일치하는 레코드가 있는지 확인한다.<br>- 인증 성공 시 서버는 성공 응답, 사용자 ID, 사용자 이름, 사용자 소속부서, 세션 ID를 반환한다.<br>- 인증 실패 시 서버는 실패 응답을 반환한다. 실패 응답은 사용자 ID 존재 여부와 암호 일치 여부를 구체적으로 구분하지 않는다.<br>- 로그인 성공 후 생성된 세션 ID는 이후 인증이 필요한 요청에서 사용한다.<br>- 로그아웃 요청을 받으면 해당 세션 ID를 무효화한다.<br>- 서버 종료 시 모든 세션은 무효화된다. |
| 이슈사항 | - 사용자 ID가 존재하지 않는 경우와 암호가 틀린 경우를 구분하여 출력하면 계정 존재 여부가 노출될 수 있다.<br>- 세션 ID 없이 클라이언트가 기능 요청을 보낼 수 있다.<br>- 동일 사용자가 여러 클라이언트에서 로그인할 수 있는지 정책이 필요하다. |
| 해결방안<br>(변경내역) | - 인증 실패 메시지는 `ID 또는 암호가 올바르지 않습니다` 수준으로 통일한다.<br>- 모든 인증 필요 요청은 세션 ID 검증 후 처리한다.<br>- 동일 사용자 다중 로그인은 과제 범위에서는 허용해도 무방하나, 세션 ID별로 독립 관리한다.<br>- 세션 ID는 서버 내부에서 생성하고 클라이언트 입력값을 그대로 신뢰하지 않는다.<br>- 변경내역: Version 1.1에서 세션 반환값과 실패 응답 정책을 명확히 함. |

### S005. 인증 상태 및 권한 검증

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S005 |
| 요구사항 명 | 인증 상태 및 권한 검증 |
| 상세 내용 | - 서버는 학생 정보 관리 기능 실행 전 세션 ID를 검증한다.<br>- 로그인하지 않은 사용자는 학생 조회, 학과별 조회, 단과대학별 조회, 학생 추가, 학생 수정, 학생 삭제 기능을 사용할 수 없다.<br>- 로그인하지 않은 사용자는 사용자 추가, 사용자 삭제 기능도 사용할 수 없다.<br>- 서버는 클라이언트 메뉴 상태를 신뢰하지 않고 요청마다 권한을 확인한다.<br>- 최초 사용자 등록은 사용자 파일에 유효 사용자가 없을 때만 인증 없이 허용한다.<br>- 서버는 인증 실패 요청에 대해 `ERR_AUTH_REQUIRED` 또는 `ERR_INVALID_SESSION`을 반환한다.<br>- 권한 검증 실패 요청은 실제 데이터 변경을 수행하지 않는다. |
| 이슈사항 | - 클라이언트가 인증 화면을 우회하여 직접 요청 메시지를 만들 수 있다.<br>- 세션 만료 또는 로그아웃 이후 이전 세션 ID가 재사용될 수 있다.<br>- 최초 사용자 등록 기능을 항상 열어두면 임의 사용자가 계정을 만들 수 있다. |
| 해결방안<br>(변경내역) | - 인증 필요 명령 목록을 서버에 두고 공통 권한 검사 함수를 적용한다.<br>- 세션 ID는 로그아웃 시 서버 세션 목록에서 제거한다.<br>- 최초 사용자 등록 가능 여부는 서버가 사용자 수를 기준으로 판단한다.<br>- 권한 검증 실패 시 데이터 파일과 연결 리스트를 변경하지 않는다.<br>- 변경내역: Version 1.1에서 권한 검사 대상 명령과 최초 등록 예외 조건을 명시함. |

### S006. 사용자 계정 추가

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S006 |
| 요구사항 명 | 사용자 계정 추가 |
| 상세 내용 | - 로그인된 사용자는 신규 직원 등 새로운 사용자를 추가할 수 있다.<br>- 사용자 추가 요청에는 사용자 ID, 암호, 소속부서, 이름이 포함되어야 한다.<br>- 사용자 ID는 기존 사용자 ID와 중복될 수 없다.<br>- 사용자 ID, 암호, 소속부서, 이름은 빈 값일 수 없다.<br>- 사용자 ID에는 탭, 줄바꿈, 메시지 구분자 등 파일 또는 메시지 포맷을 깨뜨리는 문자를 허용하지 않는다.<br>- 암호는 빈 값이 아니어야 하며 화면 또는 로그에 다시 출력하지 않는다.<br>- 사용자 추가 성공 시 서버는 `users.txt`에 새 사용자 레코드를 저장한다.<br>- 서버는 사용자 추가 성공 후 성공 응답과 추가된 사용자 ID를 반환한다.<br>- 중복 ID, 필수값 누락, 파일 저장 실패는 실패 응답으로 처리한다. |
| 이슈사항 | - 사용자 ID 중복 검사를 하지 않으면 로그인 시 어떤 계정을 기준으로 인증할지 모호하다.<br>- 탭 문자가 포함된 사용자 정보는 `users.txt`의 필드 구조를 깨뜨린다.<br>- 사용자 추가 중 파일 저장이 실패하면 클라이언트는 성공으로 오해할 수 있다. |
| 해결방안<br>(변경내역) | - 사용자 추가 전에 사용자 파일 전체 또는 메모리 사용자 목록에서 ID 중복을 검사한다.<br>- 입력값 검증 실패 시 저장을 수행하지 않는다.<br>- 파일 저장 성공까지 확인한 뒤 최종 성공 응답을 반환한다.<br>- 저장 실패 시 `ERR_FILE_WRITE`를 반환하고 기존 파일을 유지한다.<br>- 변경내역: Version 1.1에서 입력 제한 문자와 저장 성공 판정 기준을 추가함. |

### S007. 사용자 계정 삭제

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S007 |
| 요구사항 명 | 사용자 계정 삭제 |
| 상세 내용 | - 로그인된 사용자는 퇴사자 등을 고려하여 기존 사용자 계정을 삭제할 수 있다.<br>- 사용자 삭제 요청은 삭제 대상 사용자 ID를 포함해야 한다.<br>- 서버는 삭제 대상 사용자 ID가 `users.txt`에 존재하는지 확인한다.<br>- 존재하지 않는 사용자 ID이면 삭제를 수행하지 않고 실패 응답을 반환한다.<br>- 삭제 성공 시 서버는 `users.txt`에서 해당 사용자 레코드를 제거한다.<br>- 서버는 삭제 성공 후 성공 응답과 삭제된 사용자 ID를 반환한다.<br>- 삭제 대상이 현재 로그인한 사용자일 경우 즉시 삭제 허용 여부를 구현 정책으로 정의해야 한다. 본 문서의 권장 정책은 현재 로그인 사용자 삭제 제한이다.<br>- 삭제 대상이 마지막 남은 사용자일 경우 시스템 접근 불능을 방지하기 위해 삭제를 제한한다. |
| 이슈사항 | - 마지막 사용자 삭제 후에는 누구도 로그인할 수 없다.<br>- 현재 로그인 사용자를 삭제하면 현재 세션 처리 정책이 불명확해진다.<br>- 파일을 직접 수정하다 실패하면 전체 사용자 파일이 손상될 수 있다. |
| 해결방안<br>(변경내역) | - 현재 로그인 사용자 삭제 요청은 `ERR_DELETE_SELF_NOT_ALLOWED`로 거부한다.<br>- 마지막 사용자 삭제 요청은 `ERR_LAST_USER_NOT_ALLOWED`로 거부한다.<br>- 삭제는 임시 파일에 삭제 대상 제외 레코드를 기록한 뒤 성공 시 원본 파일과 교체한다.<br>- 삭제 성공 후 사용자 목록 캐시가 있다면 다시 로딩하거나 메모리에서도 제거한다.<br>- 변경내역: Version 1.1에서 자기 자신 삭제와 마지막 사용자 삭제 정책을 구체화함. |

### S008. 학생 단일 조회

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S008 |
| 요구사항 명 | 학생 단일 조회 |
| 상세 내용 | - 로그인된 사용자는 학번을 기준으로 학생 1명의 상세 정보를 조회할 수 있다.<br>- 조회 요청에는 학번이 반드시 포함되어야 한다.<br>- 서버는 연결 리스트를 처음부터 끝까지 순회하며 학번이 일치하는 학생 노드를 찾는다.<br>- 조회 성공 시 학번, 이름, 소속학과, 학년, GPA, 이메일, 모바일폰을 모두 반환한다.<br>- 조회 실패 시 `ERR_STUDENT_NOT_FOUND`를 반환한다.<br>- 학번 입력값이 비어 있거나 허용되지 않는 문자를 포함하면 `ERR_INVALID_INPUT`을 반환한다.<br>- 단일 조회 기능은 수정 및 삭제 전 대상 확인에도 사용된다. |
| 이슈사항 | - 연결 리스트 검색은 선형 검색이므로 데이터가 많을수록 시간이 증가한다.<br>- 학번 입력에 공백이 포함되면 실제 값과 비교가 실패할 수 있다.<br>- 존재하지 않는 학번과 입력 오류를 구분하지 않으면 사용자 안내가 부정확하다. |
| 해결방안<br>(변경내역) | - 입력값 앞뒤 공백은 제거한 뒤 비교한다.<br>- 학번 형식 오류와 검색 결과 없음은 서로 다른 오류 코드로 반환한다.<br>- 과제 요구사항에서 연결 리스트 사용을 요구하므로 별도 인덱스 자료구조는 필수로 추가하지 않는다.<br>- 변경내역: Version 1.1에서 조회 입력, 반환 필드, 오류 코드를 상세화함. |

### S009. 학생 정보 추가

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S009 |
| 요구사항 명 | 학생 정보 추가 |
| 상세 내용 | - 로그인된 사용자는 신규 학생 정보를 추가할 수 있다.<br>- 추가 요청에는 학번, 이름, 소속학과, 학년, GPA, 이메일, 모바일폰이 모두 포함되어야 한다.<br>- 학번은 중복될 수 없다.<br>- 이름과 소속학과는 빈 값일 수 없다.<br>- 학년은 숫자로 변환 가능해야 하며 허용 범위 내 값이어야 한다.<br>- GPA는 실수로 변환 가능해야 하며 허용 범위 내 값이어야 한다.<br>- 이메일과 모바일폰은 기본 형식 검증을 통과해야 한다.<br>- 검증을 통과하면 서버는 새 `Student` 노드를 생성하고 연결 리스트에 삽입한다.<br>- 삽입 위치는 구현 정책에 따라 리스트 끝 삽입 또는 학번 정렬 삽입을 선택할 수 있다. 기본 정책은 파일 입력 순서를 유지하기 쉬운 리스트 끝 삽입이다.<br>- 추가 성공 후 서버는 학생 정보 변경 플래그를 `true`로 설정한다.<br>- 추가 성공 후 즉시 파일 저장을 수행할지, 01시 저장 또는 종료 시 저장할지는 저장 정책에 따른다. |
| 이슈사항 | - 중복 학번 검사를 누락하면 조회, 수정, 삭제 대상이 불명확해진다.<br>- 노드 생성 후 일부 필드만 복사되면 불완전한 학생 정보가 저장된다.<br>- 추가 성공 후 파일 저장 실패가 발생할 수 있다.<br>- 사용자가 입력한 탭 또는 줄바꿈 문자가 파일 포맷을 깨뜨릴 수 있다. |
| 해결방안<br>(변경내역) | - 추가 전에 학번 중복 검색을 수행한다.<br>- 모든 필드 검증이 완료된 뒤에만 노드를 생성하거나 리스트에 연결한다.<br>- 문자열 복사는 버퍼 크기를 고려하여 수행하고 항상 종료 문자를 보장한다.<br>- 탭, 줄바꿈, 메시지 구분자는 입력 단계에서 거부한다.<br>- 저장 실패와 추가 실패를 구분하여 응답한다. 추가는 메모리에 성공했으나 저장이 실패한 경우 변경 플래그를 유지한다.<br>- 변경내역: Version 1.1에서 삽입 정책과 저장 실패 처리 기준을 보강함. |

### S010. 학생 정보 수정

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S010 |
| 요구사항 명 | 학생 정보 수정 |
| 상세 내용 | - 로그인된 사용자는 기존 학생 정보를 수정할 수 있다.<br>- 수정 대상은 학번으로 식별한다.<br>- 서버는 수정 요청 처리 전 해당 학번의 학생 노드가 존재하는지 확인한다.<br>- 수정 가능한 항목은 이름, 소속학과, 학년, GPA, 이메일, 모바일폰이다.<br>- 학번은 기본적으로 수정 불가 항목으로 둔다. 학번 변경을 허용할 경우 새 학번의 중복 검사를 반드시 수행해야 한다.<br>- 부분 수정 방식에서는 전달된 필드만 변경하고 전달되지 않은 필드는 기존 값을 유지한다.<br>- 전체 수정 방식에서는 모든 수정 가능 필드를 다시 입력받아 갱신한다.<br>- 본 문서의 권장 방식은 사용 편의성을 위해 부분 수정 방식이다.<br>- 수정 입력값은 학생 추가와 동일한 검증 규칙을 적용한다.<br>- 수정 성공 후 서버는 변경 플래그를 `true`로 설정한다. |
| 이슈사항 | - 일부 필드만 수정할 때 누락 필드를 빈 값으로 덮어쓸 위험이 있다.<br>- 학번 변경 허용 시 중복 검사와 기존 노드 재배치가 필요하다.<br>- 수정 중 잘못된 값이 하나라도 있으면 전체 수정 실패로 볼지 일부만 반영할지 정책이 필요하다. |
| 해결방안<br>(변경내역) | - 수정 요청에는 변경 대상 필드 목록을 명시한다.<br>- 전달되지 않은 필드는 기존 값을 유지한다.<br>- 하나 이상의 필드 검증 실패 시 해당 수정 요청 전체를 실패 처리하고 기존 값을 유지한다.<br>- 학번 변경은 기본 기능에서 제외한다.<br>- 변경내역: Version 1.1에서 부분 수정 방식과 실패 시 원자성 기준을 추가함. |

### S011. 학생 정보 삭제

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S011 |
| 요구사항 명 | 학생 정보 삭제 |
| 상세 내용 | - 로그인된 사용자는 학생 정보를 삭제할 수 있다.<br>- 삭제 요청에는 삭제 대상 학번이 포함되어야 한다.<br>- 서버는 연결 리스트에서 삭제 대상 학번을 검색한다.<br>- 대상 학생이 없으면 `ERR_STUDENT_NOT_FOUND`를 반환한다.<br>- 대상 학생이 있으면 해당 노드를 리스트에서 분리한다.<br>- 삭제 대상이 첫 노드인 경우 헤드 포인터를 다음 노드로 변경한다.<br>- 삭제 대상이 중간 또는 마지막 노드인 경우 이전 노드의 `next` 포인터를 삭제 대상의 다음 노드로 변경한다.<br>- 분리된 노드는 메모리 해제한다.<br>- 삭제 성공 후 서버는 변경 플래그를 `true`로 설정한다.<br>- 삭제 성공 응답에는 삭제된 학생의 학번과 이름을 포함할 수 있다. |
| 이슈사항 | - 첫 노드 삭제와 중간 노드 삭제 처리가 다르다.<br>- 삭제 후 해제된 포인터를 재참조하면 오류가 발생한다.<br>- 실수로 삭제한 데이터를 복구하는 기능은 요구사항에 없다.<br>- 파일 저장 전 서버가 종료되면 삭제 결과가 파일에 반영되지 않을 수 있다. |
| 해결방안<br>(변경내역) | - 삭제 함수는 이전 포인터와 현재 포인터를 함께 관리한다.<br>- 삭제 전 클라이언트가 대상 학생 정보를 보여주고 최종 확인을 받도록 한다.<br>- 서버는 삭제 성공 후 변경 플래그를 유지하고 저장 시점에 파일에 반영한다.<br>- 메모리 해제 후 해당 포인터는 사용하지 않는다.<br>- 변경내역: Version 1.1에서 삭제 위치별 처리와 최종 확인 흐름을 구체화함. |

### S012. 학과명 기반 학생 목록 조회

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S012 |
| 요구사항 명 | 학과명 기반 학생 목록 조회 |
| 상세 내용 | - 로그인된 사용자는 학과명을 입력하여 해당 학과 소속 전체 학생 정보를 조회할 수 있다.<br>- 조회 요청에는 학과명이 포함되어야 한다.<br>- 서버는 연결 리스트를 순회하며 학생의 소속학과와 입력 학과명을 비교한다.<br>- 비교는 앞뒤 공백 제거 후 정확 일치를 기본으로 한다.<br>- 일치하는 학생이 1명 이상이면 학생 목록과 총 건수를 반환한다.<br>- 반환 목록의 각 항목에는 학번, 이름, 학년, GPA, 이메일, 모바일폰을 포함한다.<br>- 일치하는 학생이 없으면 `ERR_NO_RESULT`를 반환한다.<br>- 결과 건수가 많을 수 있으므로 서버 응답에는 전체 건수와 현재 반환 건수를 포함한다. |
| 이슈사항 | - 학과명 표기 방식이 다르면 같은 학과라도 조회되지 않을 수 있다.<br>- 결과가 많으면 한 번에 출력하기 어렵다.<br>- 응답 메시지 크기가 커질 수 있다. |
| 해결방안<br>(변경내역) | - 기본 비교는 정확 일치로 하되 입력 앞뒤 공백을 제거한다.<br>- 필요 시 클라이언트에서 페이지 단위 출력을 적용한다.<br>- 서버 응답에는 `total_count`와 학생 배열을 포함한다.<br>- 변경내역: Version 1.1에서 반환 필드와 건수 정보를 추가함. |

### S013. 단과대학 기반 학생 목록 조회

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S013 |
| 요구사항 명 | 단과대학 기반 학생 목록 조회 |
| 상세 내용 | - 시스템은 단과대학 정보를 통한 학생 검색 기능을 제공해야 한다.<br>- 단과대학 정보 표현을 위한 구조체 이름은 반드시 `DAEHWAK`으로 정의한다.<br>- `DAEHWAK` 구조체는 단과대학명, 소속학과 목록, 소속학과 수를 포함해야 한다.<br>- 단과대학 조회 요청에는 단과대학명이 포함되어야 한다.<br>- 서버는 입력 단과대학명이 등록된 `DAEHWAK` 데이터에 존재하는지 확인한다.<br>- 단과대학이 존재하지 않으면 `ERR_COLLEGE_NOT_FOUND`를 반환한다.<br>- 단과대학이 존재하면 해당 단과대학에 포함된 학과 목록을 기준으로 학생 리스트를 검색한다.<br>- 학생의 소속학과가 `DAEHWAK.departments` 목록 중 하나와 일치하면 결과에 포함한다.<br>- 결과에는 단과대학명, 포함 학과 목록, 총 학생 수, 학생 목록을 포함한다.<br>- 단과대학은 존재하지만 소속 학생이 없으면 `ERR_NO_RESULT`를 반환한다. |
| 이슈사항 | - 학생 레코드에는 단과대학명이 직접 저장되지 않을 가능성이 높다.<br>- 단과대학별 학과 목록을 잘못 정의하면 검색 결과가 누락된다.<br>- 학과명이 변경되거나 약칭이 사용되면 비교가 실패할 수 있다.<br>- `DAEHWAK` 구조체 이름을 다르게 정의하면 명시 요구사항을 위반한다. |
| 해결방안<br>(변경내역) | - 서버 초기화 시 `DAEHWAK` 배열 또는 연결 리스트를 구성한다.<br>- 단과대학 검색은 단과대학명 확인 단계와 학생 리스트 필터링 단계로 나눈다.<br>- `DAEHWAK` 이름은 코드와 문서에서 동일하게 사용한다.<br>- 학과명은 공식 표기 기준으로 통일하고, 입력값 앞뒤 공백만 제거한다.<br>- 변경내역: Version 1.1에서 `DAEHWAK` 기반 검색 절차를 상세화함. |

### S014. 학생 정보 파일 저장

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S014 |
| 요구사항 명 | 학생 정보 파일 저장 |
| 상세 내용 | - 서버는 학생 정보 추가, 수정, 삭제로 변경된 내용을 `SMU_Students.txt`에 저장해야 한다.<br>- 서버는 학생 정보 변경 여부를 플래그로 관리한다.<br>- 서버는 매일 01시 00분에 업데이트, 추가, 삭제된 학생 정보를 파일에 저장한다.<br>- 서버 종료 시 미저장 변경 사항이 있으면 저장을 수행한다.<br>- 저장 시 연결 리스트의 모든 학생 정보를 기준 파일 형식으로 직렬화한다.<br>- 저장 형식은 `학번<TAB>이름<TAB>소속학과<TAB>학년<TAB>GPA<TAB>이메일<TAB>모바일폰`이다.<br>- 저장 성공 시 변경 플래그를 `false`로 변경한다.<br>- 저장 실패 시 변경 플래그를 유지하고 오류를 반환한다.<br>- 파일 저장은 기존 파일을 바로 덮어쓰기보다 임시 파일 작성 후 교체하는 방식을 권장한다. |
| 이슈사항 | - 01시 자동 저장은 서버가 계속 실행 중이어야 가능하다.<br>- 저장 중 프로그램이 종료되면 파일이 손상될 수 있다.<br>- 저장 실패 후 변경 플래그를 해제하면 데이터 손실이 발생한다.<br>- 매번 전체 파일을 저장하면 입출력 비용이 발생하지만, 연결 리스트 전체 상태를 일관되게 유지하기 쉽다. |
| 해결방안<br>(변경내역) | - 서버 메인 루프에서 현재 시각을 주기적으로 확인하고 01시 저장 조건을 검사한다.<br>- 날짜별로 01시 저장이 중복 실행되지 않도록 마지막 자동 저장 날짜를 기록한다.<br>- 임시 파일 `SMU_Students.tmp`에 먼저 저장하고 성공 시 원본 파일로 교체한다.<br>- 저장 실패 시 `ERR_FILE_WRITE`를 반환하고 변경 플래그를 유지한다.<br>- 변경내역: Version 1.1에서 01시 중복 저장 방지와 임시 파일 정책을 추가함. |

### S015. 서버-클라이언트 메시지 검증 및 처리

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S015 |
| 요구사항 명 | 서버-클라이언트 메시지 검증 및 처리 |
| 상세 내용 | - 서버와 클라이언트 간 요청 및 응답을 위해 일관된 메시지 포맷을 정의한다.<br>- 메시지는 요청 메시지와 응답 메시지로 구분한다.<br>- 요청 메시지는 `type`, `command`, `request_id`, `session_id`, `payload_length`, `payload` 필드를 포함한다.<br>- 응답 메시지는 `type`, `command`, `request_id`, `status`, `error_code`, `message`, `payload_length`, `payload` 필드를 포함한다.<br>- 서버는 메시지 수신 후 필수 필드 존재 여부를 먼저 검증한다.<br>- 서버는 `payload_length`와 실제 payload 문자열 길이의 일치 여부를 검증한다.<br>- 서버는 명령 코드가 지원되는 값인지 확인한다.<br>- 서버는 인증이 필요한 명령이면 세션 ID를 검증한다.<br>- 서버는 payload 내부 필드의 필수값과 형식을 검증한 뒤 업무 로직을 실행한다.<br>- 서버는 처리 결과를 표준 응답 메시지로 반환한다.<br>- 자세한 메시지 포맷은 본 문서 3장에서 정의한다. |
| 이슈사항 | - 이전 버전의 단일 행 구분자 형식은 데이터 길이 필드가 누락되어 공통 포맷과 맞지 않았다.<br>- 구분자 기반 메시지는 Markdown 표와 섞일 때 보기 어렵고, 값에 구분자가 포함되면 파싱 오류가 발생한다.<br>- 메시지 형식 오류와 업무 오류를 구분하지 않으면 디버깅이 어렵다. |
| 해결방안<br>(변경내역) | - Version 1.1부터 예시는 깨지지 않는 블록형 `key=value` 메시지로 통일한다.<br>- payload는 한 줄 문자열 또는 별도 JSON 유사 구조로 표현하되, 실제 구현에서는 C에서 파싱 가능한 `key=value` 목록으로 처리한다.<br>- 메시지 형식 오류는 `ERR_INVALID_MESSAGE`, 업무 규칙 오류는 각 기능별 오류 코드로 구분한다.<br>- 변경내역: Version 1.1에서 요청 메시지 예시를 전면 수정하고 공통 필드와 예시를 일치시킴. |

### S016. 연결 리스트 및 메모리 관리

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | S016 |
| 요구사항 명 | 연결 리스트 및 메모리 관리 |
| 상세 내용 | - 서버는 학생 정보를 연결 리스트로 관리한다.<br>- `Student` 노드 하나는 학생 1명의 모든 정보를 저장한다.<br>- 각 노드는 다음 학생 노드를 가리키는 `next` 포인터를 포함한다.<br>- 학생 추가 시 서버는 새 노드를 동적으로 생성한다.<br>- 학생 삭제 시 서버는 대상 노드를 리스트에서 분리한 뒤 메모리를 해제한다.<br>- 학생 조회, 학과별 조회, 단과대학별 조회는 연결 리스트 순회로 수행한다.<br>- 서버 종료 시 모든 학생 노드를 순회하며 메모리를 해제한다.<br>- 사용자 정보를 메모리에 올리는 경우 사용자 리스트도 같은 방식으로 해제한다.<br>- 메모리 할당 실패 시 요청은 실패 처리하고 기존 리스트 상태를 유지한다. |
| 이슈사항 | - 포인터 연결 실수는 리스트 손상 또는 메모리 접근 오류로 이어진다.<br>- 삭제 후 해제된 포인터를 사용하면 비정상 종료가 발생할 수 있다.<br>- 서버 종료 시 해제를 누락하면 메모리 누수가 발생한다.<br>- 9,000명 데이터의 선형 검색은 가능하지만 반복 조회가 많으면 성능 저하가 있다. |
| 해결방안<br>(변경내역) | - `createStudentNode`, `appendStudent`, `findStudentById`, `deleteStudentById`, `freeStudentList` 등 함수 단위로 분리한다.<br>- 삭제 함수는 첫 노드, 중간 노드, 마지막 노드 삭제를 모두 검증한다.<br>- 메모리 할당 실패 시 오류 코드를 반환하고 리스트에는 연결하지 않는다.<br>- 과제 요구사항상 연결 리스트를 사용하므로 해시 테이블 등 별도 인덱스는 추가 요구사항으로 확장하지 않는다.<br>- 변경내역: Version 1.1에서 메모리 실패와 함수 분리 기준을 추가함. |

## 2.2 클라이언트

### C001. 로그인 전 화면 및 최초 사용자 등록 화면

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | C001 |
| 요구사항 명 | 로그인 전 화면 및 최초 사용자 등록 화면 |
| 상세 내용 | - 클라이언트는 실행 후 서버 연결 상태를 확인한다.<br>- 로그인 전 화면에는 로그인, 최초 사용자 등록, 종료 메뉴를 제공할 수 있다.<br>- 최초 사용자 등록 메뉴는 서버가 허용한 경우에만 사용 가능해야 한다.<br>- 로그인 화면은 사용자 ID와 암호를 입력받는다.<br>- 암호는 가능하면 화면에 표시하지 않으며, 구현 환경상 표시 제한이 어렵다면 입력 후 다시 출력하지 않는다.<br>- 로그인 성공 시 서버가 반환한 세션 ID와 사용자 정보를 저장한다.<br>- 로그인 실패 시 로그인 전 화면으로 돌아간다.<br>- 종료 선택 시 서버 연결을 닫고 클라이언트 프로그램을 종료한다. |
| 이슈사항 | - 서버가 실행되지 않은 상태에서 클라이언트를 실행할 수 있다.<br>- 최초 사용자 등록 메뉴가 항상 보이면 기존 사용자 체계를 우회할 수 있다.<br>- 암호 입력 표시 제어는 Windows 콘솔 환경에 따라 구현 방식이 다르다. |
| 해결방안<br>(변경내역) | - 서버 연결 실패 시 “서버에 연결할 수 없습니다” 메시지를 출력하고 재시도 또는 종료를 선택하게 한다.<br>- 최초 사용자 등록 가능 여부는 서버 응답으로 판단한다.<br>- 암호 값은 클라이언트 화면, 로그, 요청 확인 메시지에 출력하지 않는다.<br>- 변경내역: Version 1.1에서 서버 연결 확인과 최초 등록 노출 조건을 상세화함. |

### C002. 로그인 후 메인 메뉴

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | C002 |
| 요구사항 명 | 로그인 후 메인 메뉴 |
| 상세 내용 | - 로그인 후 클라이언트는 학생 관리 및 사용자 관리 메뉴를 제공한다.<br>- 기본 메뉴는 `1. 학생 단일 조회`, `2. 학과별 학생 조회`, `3. 단과대학별 학생 조회`, `4. 학생 추가`, `5. 학생 수정`, `6. 학생 삭제`, `7. 사용자 추가`, `8. 사용자 삭제`, `9. 로그아웃`, `0. 종료`로 구성한다.<br>- 메뉴 번호는 숫자로 입력받는다.<br>- 정의되지 않은 번호나 문자가 입력되면 오류 메시지를 출력하고 다시 입력받는다.<br>- 각 메뉴 실행 후에는 계속 진행 가능한 상태라면 메인 메뉴로 돌아온다.<br>- 로그아웃을 선택하면 서버에 로그아웃 요청을 보내고 세션 ID를 삭제한 뒤 로그인 전 화면으로 돌아간다.<br>- 종료를 선택하면 필요 시 서버에 종료 또는 연결 종료 요청을 보내고 프로그램을 종료한다. |
| 이슈사항 | - 메뉴 입력 처리 오류로 무한 반복이나 프로그램 종료가 발생할 수 있다.<br>- 인증 만료 상태에서 메뉴가 계속 표시될 수 있다.<br>- 서버 오류 후 메뉴 복귀 흐름이 불명확할 수 있다. |
| 해결방안<br>(변경내역) | - 모든 메뉴 입력은 문자열로 받은 뒤 숫자로 변환한다.<br>- 서버가 인증 오류를 반환하면 즉시 로그인 전 화면으로 전환한다.<br>- 기능 실행 실패 후에도 클라이언트가 비정상 종료되지 않도록 공통 오류 처리 루틴을 둔다.<br>- 변경내역: Version 1.1에서 메뉴 번호와 복귀 흐름을 구체화함. |

### C003. 학생 조회 화면

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | C003 |
| 요구사항 명 | 학생 조회 화면 |
| 상세 내용 | - 클라이언트는 학생 단일 조회 메뉴에서 학번을 입력받는다.<br>- 학번 입력값이 비어 있으면 재입력을 요구한다.<br>- 클라이언트는 입력값 앞뒤 공백을 제거한다.<br>- 클라이언트는 학생 조회 요청 메시지를 생성하여 서버에 전송한다.<br>- 서버가 조회 성공을 반환하면 학생 상세 정보를 출력한다.<br>- 출력 항목은 학번, 이름, 소속학과, 학년, GPA, 이메일, 모바일폰이다.<br>- 서버가 결과 없음 오류를 반환하면 “해당 학번의 학생이 없습니다”를 출력한다.<br>- 서버가 인증 오류를 반환하면 로그인 전 화면으로 이동한다. |
| 이슈사항 | - 학번 형식을 클라이언트에서 과도하게 제한하면 실제 데이터와 맞지 않을 수 있다.<br>- 서버 응답의 일부 필드가 누락될 수 있다.<br>- 한글 출력 정렬이 콘솔 폭과 글자 폭 때문에 어긋날 수 있다. |
| 해결방안<br>(변경내역) | - 클라이언트는 빈 값과 금지 문자만 기본 검증하고 최종 검증은 서버에 맡긴다.<br>- 응답 필드 누락 시 “서버 응답 형식 오류”로 처리한다.<br>- 단일 조회 결과는 표보다 항목별 줄바꿈 출력으로 가독성을 확보한다.<br>- 변경내역: Version 1.1에서 조회 화면 입출력과 오류 출력 문구를 상세화함. |

### C004. 학생 추가 화면

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | C004 |
| 요구사항 명 | 학생 추가 화면 |
| 상세 내용 | - 클라이언트는 학생 추가 메뉴에서 학번, 이름, 소속학과, 학년, GPA, 이메일, 모바일폰을 입력받는다.<br>- 모든 항목은 필수 입력값이다.<br>- 학년은 숫자로 변환 가능한지 클라이언트에서 1차 검증한다.<br>- GPA는 실수로 변환 가능한지 클라이언트에서 1차 검증한다.<br>- 입력값에 탭, 줄바꿈, 메시지 구분자가 포함되면 재입력을 요구한다.<br>- 입력 완료 후 사용자가 입력 내용을 확인할 수 있도록 요약을 표시한다. 단, 민감정보가 아니므로 학생 정보는 출력 가능하다.<br>- 사용자가 확인하면 학생 추가 요청을 서버에 전송한다.<br>- 서버가 성공을 반환하면 “학생 정보가 추가되었습니다”를 출력한다.<br>- 서버가 중복 학번을 반환하면 “이미 존재하는 학번입니다”를 출력한다. |
| 이슈사항 | - 클라이언트에서 정상으로 보이는 값이 서버 검증에서 실패할 수 있다.<br>- GPA 범위와 학년 범위 정책이 서버와 다르면 사용자 혼란이 생긴다.<br>- 사용자가 입력 중 취소하고 싶을 수 있다. |
| 해결방안<br>(변경내역) | - 클라이언트 검증은 서버 검증과 동일한 상수 범위를 사용하도록 계획한다.<br>- 서버 검증 실패 메시지를 그대로 업무용 메시지로 매핑한다.<br>- 각 입력 단계에서 취소 명령을 둘 수 있으나, 요구사항 외 과도한 기능이 되지 않도록 단순한 “메뉴로 돌아가기” 정도로 제한한다.<br>- 변경내역: Version 1.1에서 입력 항목별 1차 검증과 확인 흐름을 추가함. |

### C005. 학생 수정 화면

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | C005 |
| 요구사항 명 | 학생 수정 화면 |
| 상세 내용 | - 클라이언트는 수정 대상 학번을 입력받는다.<br>- 먼저 학생 단일 조회 요청을 보내 대상 학생이 존재하는지 확인한다.<br>- 대상 학생이 존재하면 기존 정보를 출력한다.<br>- 사용자는 수정할 항목을 선택한다. 수정 가능 항목은 이름, 소속학과, 학년, GPA, 이메일, 모바일폰이다.<br>- 학번은 기본적으로 수정 대상에서 제외한다.<br>- 선택한 항목에 대해 새 값을 입력받고 형식을 검증한다.<br>- 여러 항목 수정이 필요한 경우 반복 입력 후 한 번에 수정 요청을 전송한다.<br>- 서버가 성공을 반환하면 수정된 정보를 다시 조회하여 출력한다.<br>- 서버가 대상 없음 또는 입력 오류를 반환하면 수정 실패 메시지를 출력한다. |
| 이슈사항 | - 조회 후 수정 사이에 다른 요청으로 데이터가 변경될 수 있다.<br>- 일부 항목만 수정할 때 나머지 필드가 빈 값으로 전송될 수 있다.<br>- 사용자가 잘못 선택한 항목을 되돌리고 싶을 수 있다. |
| 해결방안<br>(변경내역) | - 수정 요청 payload에는 실제 변경할 필드만 포함한다.<br>- 서버가 최종 대상 존재 여부와 입력값을 다시 검증한다.<br>- 클라이언트는 전송 전 변경 예정 항목을 요약해 확인받는다.<br>- 변경내역: Version 1.1에서 부분 수정 payload와 수정 전후 조회 흐름을 추가함. |

### C006. 학생 삭제 화면

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | C006 |
| 요구사항 명 | 학생 삭제 화면 |
| 상세 내용 | - 클라이언트는 삭제 대상 학번을 입력받는다.<br>- 삭제 전 서버에 학생 단일 조회 요청을 보내 대상 학생 정보를 확인한다.<br>- 대상 학생이 존재하지 않으면 삭제 요청을 보내지 않고 결과 없음 메시지를 출력한다.<br>- 대상 학생이 존재하면 학번, 이름, 소속학과, 학년을 출력하고 삭제 여부를 다시 확인한다.<br>- 사용자가 최종 확인한 경우에만 삭제 요청을 서버에 전송한다.<br>- 서버가 성공을 반환하면 삭제 완료 메시지를 출력한다.<br>- 서버가 실패를 반환하면 실패 사유를 출력한다. |
| 이슈사항 | - 삭제는 복구 기능이 요구사항에 없으므로 실수 방지가 중요하다.<br>- 조회 후 삭제 사이에 대상 학생이 이미 삭제되었을 수 있다.<br>- 최종 확인 문구가 불명확하면 사용자가 실수로 삭제할 수 있다. |
| 해결방안<br>(변경내역) | - 삭제 확인은 `Y` 입력 시에만 진행하고 그 외 입력은 취소로 처리한다.<br>- 서버는 삭제 요청 시점에 다시 대상 존재 여부를 확인한다.<br>- 삭제 완료 후 같은 학번으로 재조회하면 결과 없음이 나와야 한다.<br>- 변경내역: Version 1.1에서 삭제 전 조회와 최종 확인 절차를 명확히 함. |

### C007. 학과 및 단과대학 조회 화면

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | C007 |
| 요구사항 명 | 학과 및 단과대학 조회 화면 |
| 상세 내용 | - 클라이언트는 학과별 조회 메뉴에서 학과명을 입력받는다.<br>- 클라이언트는 단과대학별 조회 메뉴에서 단과대학명을 입력받는다.<br>- 입력값이 비어 있으면 재입력을 요구한다.<br>- 서버가 성공을 반환하면 총 건수와 학생 목록을 출력한다.<br>- 학생 목록은 학번, 이름, 소속학과, 학년, GPA 중심으로 출력하고 필요 시 이메일과 모바일폰도 함께 출력한다.<br>- 결과가 많으면 일정 건수 단위로 출력 후 계속 출력 여부를 확인한다.<br>- 서버가 결과 없음 또는 단과대학 없음 오류를 반환하면 해당 메시지를 출력한다. |
| 이슈사항 | - 많은 결과를 한 번에 출력하면 콘솔에서 확인하기 어렵다.<br>- 단과대학 조회 시 어떤 학과들이 포함되었는지 사용자가 알기 어렵다.<br>- 학과명과 단과대학명 입력 오탈자가 많을 수 있다. |
| 해결방안<br>(변경내역) | - 목록 출력은 기본 20건 단위 페이지 출력으로 계획한다.<br>- 단과대학 조회 결과에는 단과대학명과 포함 학과 목록을 먼저 출력한다.<br>- 오탈자 자동 보정은 요구사항 외 기능이므로 추가하지 않고, 정확한 명칭 입력을 안내한다.<br>- 변경내역: Version 1.1에서 페이지 출력과 단과대학 포함 학과 표시를 추가함. |

### C008. 사용자 계정 관리 화면

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | C008 |
| 요구사항 명 | 사용자 계정 관리 화면 |
| 상세 내용 | - 클라이언트는 로그인된 사용자에게 사용자 추가와 사용자 삭제 메뉴를 제공한다.<br>- 사용자 추가 화면은 사용자 ID, 암호, 소속부서, 이름을 입력받는다.<br>- 암호는 입력 후 화면에 다시 표시하지 않는다.<br>- 사용자 삭제 화면은 삭제 대상 사용자 ID를 입력받는다.<br>- 사용자 삭제 전 대상 ID와 삭제 여부를 다시 확인한다.<br>- 서버가 사용자 추가 성공을 반환하면 성공 메시지를 출력한다.<br>- 서버가 중복 사용자 ID를 반환하면 중복 안내를 출력한다.<br>- 서버가 사용자 삭제 성공을 반환하면 삭제 완료 메시지를 출력한다.<br>- 서버가 마지막 사용자 삭제 제한 또는 현재 사용자 삭제 제한을 반환하면 해당 정책 메시지를 출력한다. |
| 이슈사항 | - 사용자 삭제는 시스템 접근 권한을 제거하는 기능이므로 실수 방지가 필요하다.<br>- 암호 노출은 계정 보안 문제를 일으킨다.<br>- 사용자 추가 및 삭제가 파일에 반영되었는지 클라이언트만으로 확인하기 어렵다. |
| 해결방안<br>(변경내역) | - 사용자 삭제는 명시 확인 후에만 요청한다.<br>- 암호는 확인 요약 화면에도 표시하지 않는다.<br>- 파일 반영 여부는 서버 최종 응답을 기준으로 판단한다.<br>- 변경내역: Version 1.1에서 사용자 관리 화면의 성공/실패 메시지 기준을 구체화함. |

### C009. 서버 응답 출력 및 오류 표시

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | C009 |
| 요구사항 명 | 서버 응답 출력 및 오류 표시 |
| 상세 내용 | - 클라이언트는 서버 응답의 `status`와 `error_code`를 기준으로 성공과 실패를 구분한다.<br>- 성공 응답이면 기능별 성공 메시지와 필요한 데이터를 출력한다.<br>- 실패 응답이면 사용자에게 이해 가능한 한국어 오류 메시지를 출력한다.<br>- 인증 오류가 발생하면 저장된 세션 ID를 삭제하고 로그인 전 화면으로 이동한다.<br>- 서버 연결 실패가 발생하면 재시도 또는 종료를 선택하게 한다.<br>- 응답 메시지 형식이 잘못되었으면 “서버 응답 형식 오류”를 출력한다.<br>- 파일 저장 실패 등 서버 내부 오류는 사용자가 조치 가능한 수준으로 안내한다. |
| 이슈사항 | - 서버 오류 코드를 그대로 출력하면 사용자가 이해하기 어렵다.<br>- 기능마다 오류 처리 방식이 다르면 화면 흐름이 일관되지 않다.<br>- 서버 응답이 중간에 끊기거나 비어 있을 수 있다. |
| 해결방안<br>(변경내역) | - 오류 코드와 사용자 메시지 매핑 테이블을 둔다.<br>- 공통 응답 처리 함수를 통해 기능별 중복 처리를 줄인다.<br>- 응답 수신 실패, 파싱 실패, 인증 실패를 공통 예외 흐름으로 처리한다.<br>- 변경내역: Version 1.1에서 오류 코드 기반 출력 정책을 상세화함. |

### C010. 입력값 검증 및 재입력 흐름

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | C010 |
| 요구사항 명 | 입력값 검증 및 재입력 흐름 |
| 상세 내용 | - 클라이언트는 모든 입력을 문자열로 받은 뒤 필요한 타입으로 변환한다.<br>- 빈 값은 허용하지 않는 항목에서 재입력을 요구한다.<br>- 학년은 정수 변환 가능 여부를 확인한다.<br>- GPA는 실수 변환 가능 여부를 확인한다.<br>- 사용자 ID, 학번, 이름, 학과명, 단과대학명, 이메일, 모바일폰에는 탭과 줄바꿈을 허용하지 않는다.<br>- 메시지 payload에 사용되는 `&`, `=`, 개행 문자는 입력값에서 제한하거나 이스케이프해야 한다. 본 문서의 권장 정책은 제한이다.<br>- 메뉴 입력이 잘못되면 프로그램을 종료하지 않고 다시 입력받는다.<br>- 서버 최종 검증에서 실패하면 서버 오류 메시지에 따라 재입력 또는 메뉴 복귀를 수행한다. |
| 이슈사항 | - `scanf` 사용 시 입력 버퍼 문제로 다음 입력이 건너뛰어질 수 있다.<br>- 숫자 변환 실패 처리를 하지 않으면 잘못된 값이 저장될 수 있다.<br>- 클라이언트와 서버 검증 기준이 다르면 같은 입력에 대해 서로 다른 결과가 발생한다. |
| 해결방안<br>(변경내역) | - `fgets` 기반 한 줄 입력 후 개행 제거 방식을 권장한다.<br>- 숫자 변환은 `strtol`, `strtod` 등 변환 성공 여부를 확인할 수 있는 함수를 사용한다.<br>- 검증 범위 상수는 서버와 클라이언트에서 동일하게 정의한다.<br>- 클라이언트 검증은 사용자 편의용이고 서버 검증을 최종 기준으로 한다.<br>- 변경내역: Version 1.1에서 입력 금지 문자와 재입력 정책을 추가함. |

## 2.3 기타

### ETC001. 개발 환경 및 구현 제약

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | ETC001 |
| 요구사항 명 | 개발 환경 및 구현 제약 |
| 상세 내용 | - 프로그램은 Windows 운영체제의 PC에서 정상 동작해야 한다.<br>- 구현 언어는 C 언어로 한다.<br>- 사용자 인터페이스는 콘솔 기반이어야 한다.<br>- 서버 프로그램과 클라이언트 프로그램은 분리한다.<br>- 서버는 학생정보 관리를 위한 초기화 작업 및 클라이언트의 학생정보 관리 관련 요청을 수행하여 서비스를 제공한다.<br>- 클라이언트는 사용자의 명령을 기반으로 학생정보 관련 요청을 서버로 송신하고 결과를 사용자에게 제공한다.<br>- GUI, 웹, 모바일 앱, 데이터베이스 서버, 외부 인증 시스템은 범위에서 제외한다.<br>- 요구사항에 명시되지 않은 기능은 추가하지 않는다. |
| 이슈사항 | - 통신 방식, 빌드 방식, 실행 방식이 과제 원문에서 구체적으로 지정되지 않았다.<br>- Windows와 Linux 간 소켓, 콘솔, 인코딩 처리가 다를 수 있다.<br>- 요구사항 외 기능을 추가하면 구현 범위가 불필요하게 커진다. |
| 해결방안<br>(변경내역) | - Windows 기준 C 콘솔 프로그램으로 설계한다.<br>- 실제 통신 방식은 개발계획서에서 명확히 정의한다.<br>- 요구사항 분석서는 서버와 클라이언트 역할, 메시지 포맷, 데이터 처리를 중심으로 작성한다.<br>- 변경내역: Version 1.1에서 제외 범위와 역할 구분을 명확히 함. |

### ETC002. 학생 데이터 정의

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | ETC002 |
| 요구사항 명 | 학생 데이터 정의 |
| 상세 내용 | - 학생 데이터는 이름, 학번, 소속학과, 학년, GPA, 이메일 연락처, 모바일 폰 연락처를 포함한다.<br>- 학번은 학생의 고유 식별자로 사용한다.<br>- 이름은 학생 표시명이며 빈 값일 수 없다.<br>- 소속학과는 학과명 기반 조회와 단과대학 기반 조회에 사용한다.<br>- 학년은 정수형 데이터로 관리한다.<br>- GPA는 실수형 데이터로 관리한다.<br>- 이메일 연락처는 학생 연락 수단으로 저장한다.<br>- 모바일 폰 연락처는 학생 연락 수단으로 저장한다.<br>- 학생 데이터는 서버의 연결 리스트 노드에 저장된다.<br>- 학생 데이터는 파일 저장 시 탭 구분 텍스트로 직렬화된다. |
| 이슈사항 | - GPA와 학년의 허용 범위가 학교 정책에 따라 달라질 수 있다.<br>- 이메일과 모바일폰 형식은 엄격한 검증보다 기본 검증이 과제 범위에 적합하다.<br>- 학생 파일의 기존 필드 순서가 실제 제공 파일과 다르면 구현 전 확인이 필요하다. |
| 해결방안<br>(변경내역) | - 기본 검증 범위는 학년 1~4, GPA 0.0~4.5로 정의한다.<br>- 형식 검증은 과제 범위에 맞춰 기본 수준으로 제한한다.<br>- 실제 구현 전 `SMU_Students.txt`의 샘플 레코드를 확인하여 최종 필드 순서를 고정한다.<br>- 변경내역: Version 1.1에서 학생 데이터 사전을 상세화함. |

### ETC003. 사용자 데이터 정의

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | ETC003 |
| 요구사항 명 | 사용자 데이터 정의 |
| 상세 내용 | - 사용자 데이터는 사용자 ID, 사용자 암호, 사용자 소속부서, 사용자 이름을 포함한다.<br>- 사용자 ID는 로그인 식별자로 사용하며 중복될 수 없다.<br>- 사용자 암호는 로그인 인증에 사용한다.<br>- 사용자 소속부서는 교직원의 부서 정보를 나타낸다.<br>- 사용자 이름은 교직원 표시명으로 사용한다.<br>- 사용자 데이터는 `users.txt` 파일에서 관리한다.<br>- 사용자 추가 및 삭제 결과는 `users.txt`에 반영되어야 한다.<br>- 사용자 데이터는 탭 구분 텍스트로 저장한다. |
| 이슈사항 | - 암호 평문 저장은 보안성이 낮다.<br>- 사용자 삭제 후 로그인 세션 처리 정책이 필요하다.<br>- 사용자 ID 중복이 있으면 인증 결과가 불명확하다. |
| 해결방안<br>(변경내역) | - 과제 범위에서는 평문 파일 기반 인증을 사용하되 암호 노출을 금지한다.<br>- 사용자 ID 중복은 파일 로딩과 사용자 추가 시 모두 검증한다.<br>- 현재 로그인 사용자와 마지막 사용자는 삭제 제한을 권장한다.<br>- 변경내역: Version 1.1에서 사용자 데이터 검증과 삭제 제한 근거를 추가함. |

### ETC004. 단과대학 데이터 정의

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | ETC004 |
| 요구사항 명 | 단과대학 데이터 정의 |
| 상세 내용 | - 소속 단과대학을 위한 구조체 이름은 `DAEHWAK`으로 정의한다.<br>- `DAEHWAK` 구조체는 단과대학명, 소속학과 목록, 소속학과 수를 포함한다.<br>- 단과대학 정보는 단과대학 기반 학생 검색에 사용한다.<br>- 학생 데이터에 단과대학명이 직접 없으면 학생의 소속학과를 단과대학의 학과 목록과 비교한다.<br>- 단과대학 목록은 서버 초기화 단계에서 구성한다.<br>- 단과대학명은 사용자가 입력하는 검색 조건이다.<br>- 소속학과 목록은 학생의 `department` 필드와 비교되는 기준 데이터이다. |
| 이슈사항 | - 과제 원문은 `DAEHWAK` 구조체 이름을 지정하지만 실제 단과대학 목록 파일은 지정하지 않는다.<br>- 학과명이 공식 표기와 다르면 검색 결과가 누락된다.<br>- 단과대학 목록을 하드코딩하면 변경에 취약하다. |
| 해결방안<br>(변경내역) | - 개발계획서에서 단과대학 목록 초기화 방식을 명시한다. 과제 범위에서는 정적 배열 초기화를 권장한다.<br>- 학과명은 `SMU_Students.txt`의 소속학과 표기와 동일하게 관리한다.<br>- `DAEHWAK` 구조체 정의와 초기화 예시는 개발계획서에 포함한다.<br>- 변경내역: Version 1.1에서 단과대학 데이터의 역할과 초기화 기준을 추가함. |

### ETC005. 파일 형식 및 구분자 규칙

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | ETC005 |
| 요구사항 명 | 파일 형식 및 구분자 규칙 |
| 상세 내용 | - `users.txt`는 사용자 정보를 저장한다.<br>- `users.txt`의 필드 순서는 사용자 ID, 사용자 암호, 사용자 소속부서, 사용자 이름이다.<br>- `users.txt`의 필드는 탭 문자로 구분한다.<br>- `SMU_Students.txt`는 학생 정보를 저장한다.<br>- `SMU_Students.txt`의 권장 필드 순서는 학번, 이름, 소속학과, 학년, GPA, 이메일, 모바일폰이다.<br>- `SMU_Students.txt`의 필드는 탭 문자로 구분한다.<br>- 각 레코드는 한 줄로 저장한다.<br>- 데이터 값에는 탭과 줄바꿈을 허용하지 않는다.<br>- 파일 저장 시 마지막 줄 개행 여부는 구현 정책으로 정하되, 읽기 함수가 마지막 줄 개행 유무와 무관하게 동작해야 한다. |
| 이슈사항 | - 입력값에 탭이 포함되면 필드 수가 변한다.<br>- 직접 파일 수정으로 필드 순서가 바뀔 수 있다.<br>- 한글 인코딩이 맞지 않으면 콘솔 출력이 깨질 수 있다. |
| 해결방안<br>(변경내역) | - 입력 단계에서 탭과 줄바꿈을 금지한다.<br>- 파일 파싱 시 필드 수를 검증하고 오류 레코드로 분류한다.<br>- 개발계획서에 파일 인코딩과 콘솔 코드 페이지 정책을 명시한다.<br>- 변경내역: Version 1.1에서 레코드 단위와 구분자 금지 규칙을 명확히 함. |

### ETC006. 보안 및 권한 정책

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | ETC006 |
| 요구사항 명 | 보안 및 권한 정책 |
| 상세 내용 | - 서버는 인증이 필요한 모든 요청에 대해 세션 ID를 검증한다.<br>- 클라이언트는 로그인 상태에 따라 메뉴를 제한하지만, 최종 권한 판단은 서버가 수행한다.<br>- 암호는 화면, 로그, 오류 메시지, 확인 메시지에 출력하지 않는다.<br>- 인증 실패 메시지는 사용자 ID 존재 여부를 직접 알려주지 않는다.<br>- 사용자 추가와 삭제는 로그인된 사용자만 수행할 수 있다.<br>- 최초 사용자 등록은 유효 사용자가 0명일 때만 허용한다.<br>- 저장 파일 접근 실패 시 내부 경로나 민감한 구현 정보를 과도하게 노출하지 않는다. |
| 이슈사항 | - 파일 기반 암호 저장은 보안성이 제한적이다.<br>- 클라이언트만 권한을 제한하면 요청 위조에 취약하다.<br>- 최초 사용자 등록을 항상 허용하면 무단 계정 생성 위험이 있다. |
| 해결방안<br>(변경내역) | - 서버 공통 권한 검사 함수를 구현한다.<br>- 사용자 수가 0명일 때만 최초 사용자 등록 명령을 허용한다.<br>- 암호 출력 금지 규칙을 로그인과 사용자 추가 화면 모두에 적용한다.<br>- 변경내역: Version 1.1에서 최초 등록 보안 조건과 서버 중심 권한 검증을 보강함. |

### ETC007. 오류 코드 및 예외 처리 정책

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | ETC007 |
| 요구사항 명 | 오류 코드 및 예외 처리 정책 |
| 상세 내용 | - 서버는 오류 상황을 표준 오류 코드로 응답한다.<br>- 인증 필요 오류는 `ERR_AUTH_REQUIRED`로 정의한다.<br>- 유효하지 않은 세션은 `ERR_INVALID_SESSION`으로 정의한다.<br>- 잘못된 입력은 `ERR_INVALID_INPUT`으로 정의한다.<br>- 잘못된 메시지 형식은 `ERR_INVALID_MESSAGE`로 정의한다.<br>- 학생 없음은 `ERR_STUDENT_NOT_FOUND`로 정의한다.<br>- 검색 결과 없음은 `ERR_NO_RESULT`로 정의한다.<br>- 중복 학번은 `ERR_DUPLICATE_STUDENT_ID`로 정의한다.<br>- 중복 사용자 ID는 `ERR_DUPLICATE_USER_ID`로 정의한다.<br>- 사용자 없음은 `ERR_USER_NOT_FOUND`로 정의한다.<br>- 단과대학 없음은 `ERR_COLLEGE_NOT_FOUND`로 정의한다.<br>- 파일 읽기 실패는 `ERR_FILE_READ`로 정의한다.<br>- 파일 쓰기 실패는 `ERR_FILE_WRITE`로 정의한다.<br>- 메모리 할당 실패는 `ERR_MEMORY_ALLOC`으로 정의한다.<br>- 알 수 없는 서버 오류는 `ERR_SERVER_INTERNAL`로 정의한다. |
| 이슈사항 | - 오류 메시지를 문자열로만 처리하면 클라이언트가 원인을 분기하기 어렵다.<br>- 오류 코드가 많아질수록 누락된 매핑이 발생할 수 있다.<br>- 서버 내부 오류를 그대로 출력하면 사용자가 이해하기 어렵다. |
| 해결방안<br>(변경내역) | - 서버는 오류 코드와 사용자용 메시지를 함께 반환한다.<br>- 클라이언트는 오류 코드 기반으로 화면 흐름을 결정한다.<br>- 오류 코드 목록은 서버와 클라이언트 공통 헤더에 정의한다.<br>- 변경내역: Version 1.1에서 표준 오류 코드 목록을 추가함. |

### ETC008. 구조체 정의

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | ETC008 |
| 요구사항 명 | 구조체 정의 |
| 상세 내용 | - 계획서에는 서버에서 필요한 구조체와 구조체 멤버를 정의해야 한다.<br>- 학생 구조체는 학번, 이름, 소속학과, 학년, GPA, 이메일, 모바일폰, 다음 노드 포인터를 포함해야 한다.<br>- 사용자 구조체는 사용자 ID, 암호, 소속부서, 이름, 다음 노드 포인터를 포함할 수 있다.<br>- 단과대학 구조체 이름은 반드시 `DAEHWAK`으로 정의한다.<br>- `DAEHWAK` 구조체는 단과대학명, 소속학과 목록, 소속학과 수를 포함해야 한다.<br>- 서버 세션 관리를 구현하는 경우 세션 ID, 사용자 ID, 로그인 시각 또는 활성 여부를 저장하는 세션 구조체를 정의할 수 있다.<br>- 메시지 처리를 위해 요청 메시지와 응답 메시지를 표현하는 구조체를 정의할 수 있다. |
| 이슈사항 | - 구조체 필드 길이가 부족하면 문자열 잘림이 발생한다.<br>- 파일 형식과 구조체 필드가 일치하지 않으면 변환 로직이 복잡해진다.<br>- `DAEHWAK` 이름을 다른 이름으로 구현하면 과제 요구사항을 위반한다. |
| 해결방안<br>(변경내역) | - 문자열 필드 길이는 예상 데이터보다 여유 있게 정의하고 복사 시 버퍼 길이를 확인한다.<br>- 파일 필드 순서와 구조체 필드 매핑을 개발계획서에 명시한다.<br>- `DAEHWAK` 이름은 코드와 문서 모두에서 동일하게 사용한다.<br>- 변경내역: Version 1.1에서 세션과 메시지 구조체 후보를 추가함. |

#### 구조체 후보

```c
typedef struct Student {
    char student_id[20];
    char name[50];
    char department[100];
    int grade;
    double gpa;
    char email[100];
    char mobile[30];
    struct Student *next;
} Student;

typedef struct User {
    char user_id[50];
    char password[100];
    char department[100];
    char name[50];
    struct User *next;
} User;

typedef struct DAEHWAK {
    char college_name[100];
    char departments[50][100];
    int department_count;
    struct DAEHWAK *next;
} DAEHWAK;

typedef struct Session {
    char session_id[64];
    char user_id[50];
    int active;
    struct Session *next;
} Session;
```

### ETC009. 인수 기준 및 테스트 기준

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | ETC009 |
| 요구사항 명 | 인수 기준 및 테스트 기준 |
| 상세 내용 | - 올바른 사용자 ID와 암호로 로그인하면 로그인 성공 및 메인 메뉴가 출력되어야 한다.<br>- 틀린 암호로 로그인하면 로그인 실패 메시지가 출력되어야 한다.<br>- 로그인하지 않고 학생 관리 기능을 요청하면 인증 필요 메시지가 출력되어야 한다.<br>- 존재하는 학번으로 학생 조회 시 학생 상세 정보가 출력되어야 한다.<br>- 존재하지 않는 학번으로 조회 시 학생 없음 메시지가 출력되어야 한다.<br>- 정상 학생 정보를 추가하면 이후 같은 학번으로 조회 가능해야 한다.<br>- 중복 학번으로 추가하면 추가 실패 메시지가 출력되어야 한다.<br>- 기존 학생 정보를 수정하면 조회 결과에 수정값이 반영되어야 한다.<br>- 기존 학생을 삭제하면 이후 같은 학번으로 조회되지 않아야 한다.<br>- 학과명으로 조회하면 해당 학과 소속 학생 목록과 건수가 출력되어야 한다.<br>- 단과대학명으로 조회하면 해당 단과대학 소속 학생 목록과 건수가 출력되어야 한다.<br>- 신규 사용자 추가 후 해당 사용자로 로그인 가능해야 한다.<br>- 사용자 삭제 후 해당 사용자로 로그인할 수 없어야 한다.<br>- 01시 저장 조건 또는 서버 종료 저장 후 `SMU_Students.txt`에 변경 내용이 반영되어야 한다.<br>- 파일 형식 오류, 입력 오류, 저장 실패 상황에서 프로그램이 비정상 종료되지 않아야 한다. |
| 이슈사항 | - 서버와 클라이언트 통합 기능은 단위 테스트만으로 검증하기 어렵다.<br>- 파일 저장 테스트는 원본 데이터를 오염시킬 수 있다.<br>- 01시 자동 저장은 실제 시간을 기다리기 어렵다. |
| 해결방안<br>(변경내역) | - 테스트용 `SMU_Students.txt`와 `users.txt` 복사본을 사용한다.<br>- 파싱, 검증, 연결 리스트 조작, 로그인 검증, 메시지 파싱은 함수 단위로 테스트한다.<br>- 자동 저장은 시간 확인 함수를 분리하여 테스트 시각을 주입할 수 있게 한다.<br>- 통합 테스트는 서버 실행, 클라이언트 로그인, CRUD, 저장 확인 순서로 수행한다.<br>- 변경내역: Version 1.1에서 테스트 시나리오와 파일 오염 방지 기준을 보강함. |

### ETC010. 산출물 및 문서화 요구사항

| 항목 | 내용 |
| --- | --- |
| 요구사항 ID | ETC010 |
| 요구사항 명 | 산출물 및 문서화 요구사항 |
| 상세 내용 | - 요구사항 분석서는 제공된 양식의 `요구사항 리스트`와 `상세 요구사항 분석` 구조를 따른다.<br>- 개발계획서에는 필요한 구조체와 구조체 멤버를 정의해야 한다.<br>- 개발계획서에는 서버와 클라이언트 간 송수신 메시지 포맷을 정의해야 한다.<br>- 개발계획서에는 시스템 이해를 돕는 도식을 포함해야 한다.<br>- 필요한 도식 후보는 시스템 구성도, 서버-클라이언트 요청 흐름도, 학생 연결 리스트 구조도, 파일 입출력 흐름도이다.<br>- 산출물은 요구사항 외 기능을 추가하지 않는 범위에서 작성해야 한다. |
| 이슈사항 | - 도식이 없으면 계획서 요구사항을 충족하지 못한다.<br>- 메시지 포맷이 분석서와 개발계획서에서 다르면 구현 혼선이 발생한다.<br>- 요구사항 외 기능이 많으면 핵심 구현 시간이 부족해진다. |
| 해결방안<br>(변경내역) | - 본 분석서의 메시지 포맷을 개발계획서에 동일하게 반영한다.<br>- 도식은 요구사항을 설명하는 최소 범위로 작성한다.<br>- 요구사항 외 기능은 제외하고 학생 관리, 사용자 관리, 로그인, 파일 저장, 검색 기능에 집중한다.<br>- 변경내역: Version 1.1에서 도식 후보와 문서 일관성 기준을 추가함. |

## 3. 서버-클라이언트 메시지 포맷 정의

기존의 단일 행 구분자 예시는 공통 필드와 실제 예시가 맞지 않고, Markdown 표나 구분자와 섞이면 가독성이 떨어진다. 본 문서에서는 줄 단위 `key=value` 블록 형식을 기준 예시로 사용한다. 실제 C 구현에서는 각 줄을 읽고 `=` 기준으로 키와 값을 분리하면 된다.

### 3.1 요청 메시지 공통 형식

```text
type=REQ
command=<COMMAND>
request_id=<REQUEST_ID>
session_id=<SESSION_ID 또는 NONE>
payload_length=<PAYLOAD 문자열 길이>
payload=<key1=value1&key2=value2>
end=MSG
```

| 필드 | 필수 | 설명 |
| --- | --- | --- |
| `type` | 예 | 요청 메시지는 항상 `REQ` |
| `command` | 예 | 서버가 수행할 명령 코드 |
| `request_id` | 예 | 요청과 응답을 대응하기 위한 클라이언트 생성 ID |
| `session_id` | 조건부 | 로그인 전 요청은 `NONE`, 로그인 후 요청은 서버 발급 세션 ID |
| `payload_length` | 예 | `payload` 값의 문자 길이 또는 바이트 길이. 구현 시 하나로 통일 |
| `payload` | 조건부 | 명령별 데이터. 데이터가 없으면 빈 문자열 |
| `end` | 예 | 메시지 끝 표시 |

### 3.2 응답 메시지 공통 형식

응답 메시지는 서버가 클라이언트 요청을 처리한 뒤 반드시 반환하는 표준 결과 형식이다. 클라이언트는 `request_id`로 원 요청과 응답을 대응시키고, `status`와 `error_code`를 기준으로 화면 흐름을 결정한다.

```text
type=RES
command=<COMMAND>
request_id=<REQUEST_ID>
status=<SUCCESS 또는 FAIL>
error_code=<NONE 또는 오류 코드>
message=<사용자 출력 가능 메시지>
payload_length=<PAYLOAD 문자열 길이>
payload=<key1=value1&key2=value2>
end=MSG
```

| 필드 | 필수 | 설명 |
| --- | --- | --- |
| `type` | 예 | 응답 메시지 구분값. 항상 `RES`로 설정한다. |
| `command` | 예 | 서버가 처리한 명령 코드. 요청 메시지의 `command`와 동일해야 한다. |
| `request_id` | 예 | 요청과 응답을 연결하는 식별자. 요청 메시지의 `request_id`를 그대로 반환한다. |
| `status` | 예 | 처리 결과. 정상 처리 시 `SUCCESS`, 실패 시 `FAIL`을 사용한다. |
| `error_code` | 예 | 성공 시 `NONE`, 실패 시 4장의 표준 오류 코드를 사용한다. |
| `message` | 예 | 클라이언트가 사용자에게 출력하거나 내부 매핑에 사용할 수 있는 짧은 결과 메시지이다. |
| `payload_length` | 예 | `payload` 값의 길이. 3.1과 동일하게 문자 길이 또는 바이트 길이 중 하나로 통일한다. |
| `payload` | 조건부 | 성공 시 조회 결과, 세션 정보, 저장 결과 등을 담는다. 실패 시 기본값은 빈 문자열이다. |
| `end` | 예 | 메시지 종료 표시. 항상 `MSG`로 설정한다. |

응답 처리 규칙은 다음과 같다.

| 구분 | 규칙 |
| --- | --- |
| 성공 응답 | `status=SUCCESS`, `error_code=NONE`이어야 한다. |
| 실패 응답 | `status=FAIL`, `error_code`는 `NONE`이 아닌 표준 오류 코드여야 한다. |
| 빈 payload | 반환 데이터가 없으면 `payload_length=0`, `payload=`로 작성한다. |
| 인증 오류 | `ERR_AUTH_REQUIRED` 또는 `ERR_INVALID_SESSION`이면 클라이언트는 세션을 폐기하고 로그인 전 화면으로 이동한다. |
| 메시지 오류 | 필수 필드 누락, 길이 불일치, 알 수 없는 명령은 `ERR_INVALID_MESSAGE`로 응답한다. |
| 업무 오류 | 중복 학번, 학생 없음, 파일 저장 실패 등은 각 업무 오류 코드로 응답한다. |

### 3.3 명령 코드 목록

명령 코드는 서버가 어떤 업무 로직을 실행할지 결정하는 값이다. 서버와 클라이언트는 동일한 명령 코드 상수를 사용해야 하며, 알 수 없는 명령 코드는 `ERR_INVALID_MESSAGE`로 거부한다.

| 명령 코드 | 인증 필요 | 요청 payload | 성공 payload | 대표 실패 코드 |
| --- | --- | --- | --- | --- |
| `LOGIN` | 아니오 | `user_id`, `password` | `session_id`, `user_id`, `name`, `department` | `ERR_INVALID_LOGIN`, `ERR_INVALID_INPUT` |
| `FIRST_USER_REGISTER` | 조건부 | `user_id`, `password`, `department`, `name` | `user_id` | `ERR_DUPLICATE_USER_ID`, `ERR_INVALID_INPUT`, `ERR_FILE_WRITE` |
| `LOGOUT` | 예 | 없음 또는 `session_id` | 없음 | `ERR_INVALID_SESSION` |
| `USER_ADD` | 예 | `user_id`, `password`, `department`, `name` | `user_id` | `ERR_AUTH_REQUIRED`, `ERR_DUPLICATE_USER_ID`, `ERR_FILE_WRITE` |
| `USER_DELETE` | 예 | `user_id` | `user_id` | `ERR_USER_NOT_FOUND`, `ERR_DELETE_SELF_NOT_ALLOWED`, `ERR_LAST_USER_NOT_ALLOWED` |
| `STUDENT_GET` | 예 | `student_id` | `student_id`, `name`, `department`, `grade`, `gpa`, `email`, `mobile` | `ERR_STUDENT_NOT_FOUND`, `ERR_INVALID_INPUT` |
| `STUDENT_ADD` | 예 | `student_id`, `name`, `department`, `grade`, `gpa`, `email`, `mobile` | `student_id` | `ERR_DUPLICATE_STUDENT_ID`, `ERR_INVALID_INPUT`, `ERR_MEMORY_ALLOC` |
| `STUDENT_UPDATE` | 예 | `student_id`와 변경 필드 | 변경된 학생 전체 정보 | `ERR_STUDENT_NOT_FOUND`, `ERR_INVALID_INPUT` |
| `STUDENT_DELETE` | 예 | `student_id` | `student_id`, `name` | `ERR_STUDENT_NOT_FOUND`, `ERR_INVALID_INPUT` |
| `DEPARTMENT_LIST` | 예 | `department` | `department`, `total_count`, `students` | `ERR_NO_RESULT`, `ERR_INVALID_INPUT` |
| `COLLEGE_LIST` | 예 | `college` | `college`, `departments`, `total_count`, `students` | `ERR_COLLEGE_NOT_FOUND`, `ERR_NO_RESULT` |
| `SAVE` | 예 | 없음 | `saved_count`, `saved_at` | `ERR_FILE_WRITE`, `ERR_AUTH_REQUIRED` |

명령별 처리 원칙은 다음과 같다.

| 원칙 | 내용 |
| --- | --- |
| 인증 우선 | 인증이 필요한 명령은 payload 검증보다 세션 검증을 먼저 수행한다. |
| 입력 검증 | 필수 payload가 누락되거나 형식이 잘못되면 업무 로직을 실행하지 않고 `ERR_INVALID_INPUT`을 반환한다. |
| 데이터 변경 | `USER_ADD`, `USER_DELETE`, `STUDENT_ADD`, `STUDENT_UPDATE`, `STUDENT_DELETE`, `SAVE`는 데이터 변경 명령으로 분류한다. |
| 조회 명령 | `STUDENT_GET`, `DEPARTMENT_LIST`, `COLLEGE_LIST`는 데이터를 변경하지 않는다. |
| 저장 정책 | 학생 추가, 수정, 삭제는 메모리 데이터를 먼저 변경하고 변경 플래그를 설정한다. 실제 파일 반영은 `SAVE`, 01시 자동 저장, 서버 종료 저장에서 수행한다. |
| 최초 등록 | `FIRST_USER_REGISTER`는 유효 사용자가 0명일 때만 허용하며, 사용자가 1명 이상이면 실패 응답을 반환한다. |

### 3.4 요청 메시지 예시

#### 로그인 요청

```text
type=REQ
command=LOGIN
request_id=REQ-0001
session_id=NONE
payload_length=33
payload=user_id=staff01&password=pass1234
end=MSG
```

#### 학생 단일 조회 요청

```text
type=REQ
command=STUDENT_GET
request_id=REQ-0002
session_id=SID-20260427-0001
payload_length=19
payload=student_id=20260001
end=MSG
```

#### 학생 추가 요청

```text
type=REQ
command=STUDENT_ADD
request_id=REQ-0003
session_id=SID-20260427-0001
payload_length=130
payload=student_id=20260002&name=HongGilDong&department=ComputerEngineering&grade=2&gpa=4.1&email=student@example.com&mobile=010-0000-0000
end=MSG
```

#### 학생 수정 요청

```text
type=REQ
command=STUDENT_UPDATE
request_id=REQ-0004
session_id=SID-20260427-0001
payload_length=114
payload=student_id=20260002&department=SoftwareEngineering&grade=3&gpa=4.2&email=student2@example.com&mobile=010-1111-2222
end=MSG
```

#### 학생 삭제 요청

```text
type=REQ
command=STUDENT_DELETE
request_id=REQ-0005
session_id=SID-20260427-0001
payload_length=19
payload=student_id=20260002
end=MSG
```

#### 학과별 조회 요청

```text
type=REQ
command=DEPARTMENT_LIST
request_id=REQ-0006
session_id=SID-20260427-0001
payload_length=30
payload=department=ComputerEngineering
end=MSG
```

#### 단과대학별 조회 요청

```text
type=REQ
command=COLLEGE_LIST
request_id=REQ-0007
session_id=SID-20260427-0001
payload_length=26
payload=college=EngineeringCollege
end=MSG
```

#### 사용자 추가 요청

```text
type=REQ
command=USER_ADD
request_id=REQ-0008
session_id=SID-20260427-0001
payload_length=74
payload=user_id=staff02&password=temp1234&department=AcademicAffairs&name=KimStaff
end=MSG
```

#### 사용자 삭제 요청

```text
type=REQ
command=USER_DELETE
request_id=REQ-0009
session_id=SID-20260427-0001
payload_length=15
payload=user_id=staff02
end=MSG
```

### 3.5 응답 메시지 예시

#### 로그인 성공 응답

```text
type=RES
command=LOGIN
request_id=REQ-0001
status=SUCCESS
error_code=NONE
message=login_success
payload_length=85
payload=session_id=SID-20260427-0001&user_id=staff01&name=KimStaff&department=AcademicAffairs
end=MSG
```

#### 로그인 실패 응답

```text
type=RES
command=LOGIN
request_id=REQ-0001
status=FAIL
error_code=ERR_INVALID_LOGIN
message=invalid_user_id_or_password
payload_length=0
payload=
end=MSG
```

#### 학생 조회 성공 응답

```text
type=RES
command=STUDENT_GET
request_id=REQ-0002
status=SUCCESS
error_code=NONE
message=student_found
payload_length=130
payload=student_id=20260001&name=HongGilDong&department=ComputerEngineering&grade=2&gpa=4.1&email=student@example.com&mobile=010-0000-0000
end=MSG
```

#### 중복 학번 실패 응답

```text
type=RES
command=STUDENT_ADD
request_id=REQ-0003
status=FAIL
error_code=ERR_DUPLICATE_STUDENT_ID
message=duplicate_student_id
payload_length=0
payload=
end=MSG
```

#### 학과별 조회 성공 응답

```text
type=RES
command=DEPARTMENT_LIST
request_id=REQ-0006
status=SUCCESS
error_code=NONE
message=department_students_found
payload_length=106
payload=department=ComputerEngineering&total_count=2&students=20260001,HongGilDong,2,4.1;20260003,LeeSunMoon,3,3.8
end=MSG
```

#### 인증 실패 응답

```text
type=RES
command=STUDENT_GET
request_id=REQ-0010
status=FAIL
error_code=ERR_AUTH_REQUIRED
message=login_required
payload_length=0
payload=
end=MSG
```

### 3.6 Payload 작성 규칙

| 규칙 | 내용 |
| --- | --- |
| 필드 구분 | payload 내부 필드는 `&`로 구분한다. |
| 키-값 구분 | 키와 값은 `=`로 구분한다. |
| 금지 문자 | 값에는 `&`, `=`, 탭, 줄바꿈을 허용하지 않는다. |
| 한글 값 | 실제 구현에서 한글 값을 사용할 경우 파일 인코딩과 콘솔 인코딩을 통일한다. |
| 목록 표현 | 학생 목록은 `students=` 뒤에 세미콜론으로 학생 단위를 구분한다. |
| 길이 계산 | `payload_length`는 구현 시 바이트 길이 또는 문자 길이 중 하나로 통일하고 서버와 클라이언트가 동일하게 사용한다. |

## 4. 오류 코드 정의

| 오류 코드 | 의미 | 대표 발생 위치 |
| --- | --- | --- |
| `NONE` | 오류 없음 | 성공 응답 |
| `ERR_INVALID_LOGIN` | 사용자 ID 또는 암호 불일치 | 로그인 |
| `ERR_AUTH_REQUIRED` | 로그인 필요 | 인증 필요 기능 전체 |
| `ERR_INVALID_SESSION` | 세션 없음 또는 만료 | 인증 필요 기능 전체 |
| `ERR_INVALID_MESSAGE` | 메시지 형식 오류 | 메시지 수신 |
| `ERR_INVALID_INPUT` | 입력값 오류 | 추가, 수정, 검색 |
| `ERR_FILE_READ` | 파일 읽기 실패 | 서버 초기화 |
| `ERR_FILE_WRITE` | 파일 쓰기 실패 | 저장, 사용자 추가/삭제 |
| `ERR_DUPLICATE_USER_ID` | 사용자 ID 중복 | 사용자 추가 |
| `ERR_USER_NOT_FOUND` | 사용자 없음 | 사용자 삭제 |
| `ERR_DELETE_SELF_NOT_ALLOWED` | 현재 로그인 사용자 삭제 제한 | 사용자 삭제 |
| `ERR_LAST_USER_NOT_ALLOWED` | 마지막 사용자 삭제 제한 | 사용자 삭제 |
| `ERR_DUPLICATE_STUDENT_ID` | 학번 중복 | 학생 추가 |
| `ERR_STUDENT_NOT_FOUND` | 학생 없음 | 조회, 수정, 삭제 |
| `ERR_NO_RESULT` | 목록 검색 결과 없음 | 학과/단과대학 조회 |
| `ERR_COLLEGE_NOT_FOUND` | 단과대학 없음 | 단과대학 조회 |
| `ERR_MEMORY_ALLOC` | 메모리 할당 실패 | 노드 생성 |
| `ERR_SERVER_INTERNAL` | 기타 서버 내부 오류 | 공통 |

## 5. 데이터 사전

### 5.1 학생 데이터

| 필드명 | 자료형 후보 | 필수 | 중복 | 설명 | 검증 기준 |
| --- | --- | --- | --- | --- | --- |
| `student_id` | `char[20]` | 예 | 불가 | 학번 | 빈 값 불가, 탭/줄바꿈 금지 |
| `name` | `char[50]` | 예 | 가능 | 학생 이름 | 빈 값 불가 |
| `department` | `char[100]` | 예 | 가능 | 소속학과 | 빈 값 불가 |
| `grade` | `int` | 예 | 가능 | 학년 | 정수, 기본 1~4 |
| `gpa` | `double` | 예 | 가능 | 평균학점 | 실수, 기본 0.0~4.5 |
| `email` | `char[100]` | 예 | 가능 | 이메일 연락처 | 빈 값 불가, 기본 이메일 형식 |
| `mobile` | `char[30]` | 예 | 가능 | 모바일 연락처 | 빈 값 불가, 숫자/하이픈 중심 |

### 5.2 사용자 데이터

| 필드명 | 자료형 후보 | 필수 | 중복 | 설명 | 검증 기준 |
| --- | --- | --- | --- | --- | --- |
| `user_id` | `char[50]` | 예 | 불가 | 로그인 ID | 빈 값 불가, 탭/줄바꿈 금지 |
| `password` | `char[100]` | 예 | 가능 | 로그인 암호 | 빈 값 불가, 화면 출력 금지 |
| `department` | `char[100]` | 예 | 가능 | 사용자 소속부서 | 빈 값 불가 |
| `name` | `char[50]` | 예 | 가능 | 사용자 이름 | 빈 값 불가 |

### 5.3 단과대학 데이터

| 필드명 | 자료형 후보 | 필수 | 설명 |
| --- | --- | --- | --- |
| `college_name` | `char[100]` | 예 | 단과대학명 |
| `departments` | `char[50][100]` | 예 | 해당 단과대학에 포함된 학과명 목록 |
| `department_count` | `int` | 예 | 소속학과 수 |
| `next` | `DAEHWAK *` | 선택 | 단과대학 목록을 연결 리스트로 관리할 때 사용 |

## 6. 인수 기준

| ID | 시나리오 | 사전 조건 | 기대 결과 |
| --- | --- | --- | --- |
| AC-001 | 서버 실행 | `SMU_Students.txt`, `users.txt` 준비 | 학생 정보와 사용자 정보를 로딩하고 요청 대기 상태가 된다. |
| AC-002 | 정상 로그인 | 유효 사용자 존재 | 로그인 성공, 세션 ID 반환, 메인 메뉴 출력 |
| AC-003 | 로그인 실패 | 잘못된 암호 입력 | 실패 메시지 출력, 세션 미발급 |
| AC-004 | 미로그인 학생 조회 | 세션 없음 | `ERR_AUTH_REQUIRED` 반환 |
| AC-005 | 학생 단일 조회 성공 | 학번 존재 | 학생 상세 정보 출력 |
| AC-006 | 학생 단일 조회 실패 | 학번 없음 | 학생 없음 메시지 출력 |
| AC-007 | 학생 추가 성공 | 중복 없는 학번 | 연결 리스트에 노드 추가, 변경 플래그 설정 |
| AC-008 | 학생 추가 실패 | 중복 학번 | 추가 거부, 기존 데이터 유지 |
| AC-009 | 학생 수정 성공 | 대상 학번 존재 | 변경 필드 반영, 재조회 시 수정값 출력 |
| AC-010 | 학생 수정 실패 | 대상 학번 없음 | 수정 거부, 기존 데이터 유지 |
| AC-011 | 학생 삭제 성공 | 대상 학번 존재 | 노드 삭제, 재조회 시 결과 없음 |
| AC-012 | 학과별 조회 | 해당 학과 학생 존재 | 목록과 총 건수 출력 |
| AC-013 | 단과대학별 조회 | 단과대학 존재 | 포함 학과 기준 학생 목록 출력 |
| AC-014 | 사용자 추가 성공 | 중복 없는 사용자 ID | `users.txt`에 새 사용자 저장 |
| AC-015 | 사용자 삭제 성공 | 삭제 대상 사용자 존재 | `users.txt`에서 사용자 제거 |
| AC-016 | 마지막 사용자 삭제 제한 | 사용자 1명만 존재 | 삭제 거부 |
| AC-017 | 학생 정보 저장 | 변경 플래그 true | `SMU_Students.txt`에 변경 사항 반영 |
| AC-018 | 파일 오류 처리 | 파일 쓰기 실패 상황 | 오류 메시지 출력, 변경 플래그 유지 |

## 7. 요구사항 추적표

| 원 요구사항 | 반영된 요구사항 ID |
| --- | --- |
| Windows PC에서 정상 동작하는 C 언어 콘솔 프로그램 | ETC001 |
| 서버와 클라이언트 프로그램 구성 | S001, S015, C001, C002, C009, ETC001 |
| 로그인 과정 및 인가된 사용자만 기능 사용 | S004, S005, C001, C002, ETC006 |
| 로그인된 사용자만 신규 사용자 추가 가능 | S005, S006, C008, ETC006 |
| 퇴사자 고려 사용자 삭제 기능 | S007, C008 |
| 사용자 정보는 `users.txt`에서 탭 구분자로 관리 | S003, S006, S007, ETC003, ETC005 |
| 학생 정보는 `SMU_Students.txt`에 저장 | S002, S014, ETC002, ETC005 |
| 학생 정보 항목: 이름, 학번, 학과, 학년, GPA, 이메일, 모바일 | S002, S008, S009, S010, ETC002 |
| 학생 정보 검색, 삭제, 추가, 정보변경 | S008, S009, S010, S011, C003, C004, C005, C006 |
| 학과명을 통한 학과 소속 전체 학생 출력 | S012, C007 |
| 단과대학 정보를 통한 검색 기능 | S013, C007, ETC004 |
| 단과대학 구조체 이름 `DAEHWAK` | S013, ETC004, ETC008 |
| 학생 9,000명 및 연결 리스트 관리 | S002, S016 |
| 적절한 메뉴와 인터페이스 제공 | C001, C002, C003, C004, C005, C006, C007, C008, C009, C010 |
| 서버-클라이언트 송수신 메시지 포맷 정의 | S015, 3장 |
| 필요한 구조체 및 구조체 멤버 정의 | ETC008 |
| 요구사항 외 기능 추가 금지 | ETC001, ETC010 |
| 계획서 내 필요한 도식 추가 | ETC010 |
