# 수정사항

## 0. `ref.txt`와 캡쳐 이미지 차이 점검

| 구분 | `ref.txt`에서 확인된 내용 | 캡쳐 이미지에서 확인된 내용 | 최종 처리 |
| --- | --- | --- | --- |
| 단과대학/`DAEHWAK` | 첫 요구사항 앞에 "소속 단과대학을 위한 구조체 이름을 `DAEHWAK`으로 정의하고, 단과대학 정보를 통한 검색 기능 필요" 문장이 포함되어 있음 | 캡쳐 이미지의 시스템 사용자 요구사항에는 해당 문장이 보이지 않음 | `요구사항분석서.md`, `개발계획서.md`에서 단과대학 검색과 `DAEHWAK` 구조체를 제거함 |
| 사용자정보 없음 시 회원가입 | `ref.txt`에는 "원정보가 없다면 로그인 인터페이스에 회원가입 기능 추가" 문장이 섞여 있음 | 캡쳐 이미지에는 로그인 기능과 로그인된 사용자만 신규 사용자 추가 가능하다는 요구만 보이고, 회원가입/최초 등록 요구는 보이지 않음 | 회원가입, 최초 사용자 등록, 사용자정보 없음 시 가입 메뉴를 제거함 |
| `users.txt` 탭 구분자 설명 | `ref.txt`에는 "연속된 3개의 탭으로 구분자 사용" 문장이 섞여 있음 | 캡쳐 이미지에는 사용자정보를 탭 문자를 구분자로 관리한다고만 보임 | 연속된 3개의 탭이라는 표현은 제거하고, 일반 탭 구분 파일로만 정의함 |
| 기타 사항 3번째 항목 | `ref.txt`에는 생성형 AI, 외부 자료, 출처 명시, 30% 감점 관련 문장이 포함되어 있음 | 캡쳐 이미지에도 해당 3번째 항목과 하위 문장이 보임 | 사용자 지시에 따라 3번째 항목 전체를 요구사항 분석서와 개발계획서 반영 대상에서 제외함 |
| OCR/추출 잡음 | `ref.txt`에는 `¡`, `회`, `원정보`, `01시 00분)`처럼 추출 과정에서 섞인 문자가 있음 | 캡쳐 이미지에는 일반 글머리표와 정상 문장으로 보임 | 문서에는 추출 잡음을 반영하지 않고 캡쳐에서 읽히는 정상 문장 기준으로 작성함 |
| 학생정보 검색 기준 | 기존 PDF 추출 기반 문서에는 학번 검색 등 세부 기준이 섞여 있었음 | 캡쳐 이미지에는 "학생 정보 검색"만 있고 세부 검색 기준은 고정되어 있지 않음 | 제출 문서에서는 `학번` 단정 표현을 `검색값`으로 완화함 |
| 송수신 메시지 상세 필드 | 기존 문서에는 `request_id`, `session_id`, `payload_length`, `payload`, 오류 코드 체계가 포함되어 있었음 | 캡쳐 이미지에는 메시지 포맷을 정의해야 한다는 요구만 있고 구체 필드는 없음 | 간단한 `REQ|명령|인자...`, `RES|명령|결과|메시지|데이터...` 형식으로 축소함 |

정리하면, `ref.txt`는 이전 PDF 추출 결과라 캡쳐 이미지와 다른 내용이 일부 섞여 있었다. 최종 제출 문서인 `요구사항분석서.md`와 `개발계획서.md`는 캡쳐 이미지에서 확인되는 요구사항만 기준으로 작성했고, 차이점과 제거 사유는 이 문서에만 기록했다.

## 1. 수정 기준

| 구분 | 내용 |
| --- | --- |
| 기준 자료 | 사용자가 제공한 캡쳐 이미지 |
| 참고 자료 | `ref.txt` |
| 수정 대상 | `요구사항분석서.md`, `개발계획서.md` |
| 신규 작성 | `수정사항.md` |
| 제외 조건 | 기타 사항 및 감점 사항의 3번째 항목은 사용자 지시에 따라 반영하지 않음 |

`ref.txt`는 이전 PDF 추출 내용 전체를 확인하기 위한 참고 자료로만 사용했다. 실제 문서 반영 기준은 캡쳐 이미지에 보이는 요구사항으로 제한했다.

## 2. 공통 수정사항

| 항목 | 기존 내용 | 수정 내용 | 사유 |
| --- | --- | --- | --- |
| 기준 자료 | `requirements.md`, PDF 추출 내용 중심 | 캡쳐 이미지 기준 과제 요구사항으로 변경 | PDF 추출 결과가 이미지 내용과 다른 부분이 있어 이미지 기준으로 재작성 필요 |
| 요구사항 범위 | 단과대학 검색, `DAEHWAK`, 최초 사용자 등록, 세션 ID, 로그아웃, 상세 오류 코드, `payload_length` 등 포함 | 캡쳐 이미지에 보이는 기능만 유지 | "이미지를 통해 분석한 내용이 아닌 다른 내용은 제거" 지시 반영 |
| 메시지 포맷 | `key=value`, `request_id`, `session_id`, `payload_length`, 오류 코드 기반 포맷 | `REQ|명령|인자...`, `RES|명령|결과|메시지|데이터...` 형식으로 단순화 | 캡쳐는 메시지 포맷 정의만 요구하며 세부 필드는 지정하지 않음 |
| 검색 기능 | 학번 검색, 단과대학 검색, 세부 오류 코드까지 포함 | 학생정보 검색은 `검색값` 기반으로 표현하고, 학과명 기반 전체 출력만 별도 정의 | 캡쳐에 있는 "학생 정보 검색"과 "학과명 전체 출력"만 반영 |
| 사용자 관리 | 최초 사용자 등록, 회원가입, 마지막 사용자 삭제 제한, 자기 자신 삭제 제한 포함 | 로그인된 사용자의 신규 사용자 추가와 퇴사자 고려 사용자 삭제만 유지 | 캡쳐에 직접 보이는 사용자 추가/삭제 요구사항으로 축소 |
| 외부 기능 | GUI, 웹, DB, 모바일 앱 등의 제외 범위가 길게 설명됨 | 요구사항 외 기능 추가 금지 원칙만 유지 | 불필요한 범위 설명을 제거하고 캡쳐 기준으로 정리 |
| 기타 감점 사항 | AI/외부 자료 활용 및 출처 관련 항목 일부 반영 가능성 | 기타 사항 3번째 항목은 문서 반영 대상에서 제외 | 사용자 지시 반영 |

## 3. `요구사항분석서.md` 수정사항

| 구분 | 수정 내용 |
| --- | --- |
| 문서 메타데이터 | 버전을 `Version 2.0`, 작성일자를 `2026.04.29`, 기준 자료를 `2026학년도 중간고사 대체 과제 요구사항`으로 정리 |
| 요구사항 리스트 | 서버 요구사항 14개, 클라이언트 요구사항 9개, 기타 요구사항 7개로 재구성 |
| 서버 요구사항 | 서버 초기화, 학생정보 파일 로딩, 사용자정보 파일 관리, 로그인 인증, 사용자 추가/삭제, 학생정보 검색/추가/삭제/변경, 학과명 조회, 01시 저장, 연결 리스트, 메시지 처리만 남김 |
| 클라이언트 요구사항 | 로그인, 메뉴, 학생정보 검색/추가/삭제/변경, 학과명 조회, 사용자 추가/삭제, 요청 전송 및 결과 출력으로 재정리 |
| 기타 요구사항 | Windows C 콘솔, 학생/사용자 데이터 항목, 탭 구분자, 도식 작성, 구조체 정의, 요구사항 외 기능 금지로 제한 |
| 메시지 포맷 | 요청/응답 메시지 형식과 명령 목록을 캡쳐 요구사항 수준으로 단순화 |
| 데이터 사전 | 학생 데이터와 사용자 데이터만 유지 |
| 인수 기준 | 이미지 기준 기능 검증 항목 13개로 재작성 |
| 추적표 | 캡쳐 이미지의 각 요구사항이 문서 어느 위치에 반영되었는지 다시 작성 |

### `요구사항분석서.md`에서 제거한 주요 내용

| 제거 내용 | 제거 사유 |
| --- | --- |
| 단과대학 기반 검색 요구사항 | 캡쳐 이미지에 해당 기능이 보이지 않음 |
| `DAEHWAK` 구조체 | 캡쳐 이미지에 해당 구조체명이 보이지 않음 |
| 최초 사용자 등록/회원가입 | 캡쳐 이미지에 해당 기능이 보이지 않음 |
| 로그아웃 및 세션 ID 정책 | 캡쳐 이미지에 직접 명시되지 않음 |
| 상세 오류 코드 목록 | 캡쳐 이미지에 오류 코드 정의 요구가 없음 |
| `request_id`, `session_id`, `payload_length`, `payload` 기반 메시지 | 캡쳐 이미지에 구체 필드가 명시되지 않음 |
| 단과대학 데이터 사전 | 단과대학 기능 제거에 따라 삭제 |
| 과도한 보안 정책과 확장 정책 | 요구사항 외 기능 추가 금지 원칙에 맞춰 제거 |

## 4. `개발계획서.md` 수정사항

| 구분 | 수정 내용 |
| --- | --- |
| 프로젝트 개요 | 캡쳐 이미지 기준의 필요성, 목표, 데이터 파일, 서버/클라이언트 역할로 재작성 |
| 시스템 구성도 | 클라이언트, 서버, 데이터 파일 관계를 캡쳐 요구사항 기준으로 다시 작성 |
| 요청 처리 흐름도 | 사용자 메뉴 선택부터 서버 처리, 응답 출력까지의 흐름을 추가 |
| 학생 연결 리스트 구조도 | 9,000명 학생정보를 서버 연결 리스트로 관리하는 구조를 도식화 |
| 01시 파일 저장 흐름도 | 서버에서 업데이트 및 추가/삭제된 학생정보를 매일 01시 00분 저장하는 흐름을 도식화 |
| 서버 개발 계획 | 초기화, 로그인 인증, 사용자 추가/삭제, 학생정보 검색/추가/삭제/변경, 학과명 조회, 01시 저장, 메시지 처리로 재구성 |
| 클라이언트 개발 계획 | 로그인 화면, 메뉴, 입력 검증, 요청 생성, 결과 출력으로 재구성 |
| 구조체 정의 | `Student`, `User`, `ServerState`만 정의 |
| 메시지 포맷 | `|` 구분자 기반 요청/응답 포맷과 명령별 예시를 다시 작성 |
| 추진 계획 | 캡쳐 요구사항 구현 순서에 맞춰 일정과 역할을 재작성 |
| 테스트 계획 | 캡쳐 요구사항 기준 테스트 항목 11개로 재작성 |

### `개발계획서.md`에서 제거한 주요 내용

| 제거 내용 | 제거 사유 |
| --- | --- |
| 단과대학 검색 계획 | 캡쳐 이미지에 해당 기능이 보이지 않음 |
| `DAEHWAK` 구조체 정의 | 캡쳐 이미지 기준 요구사항이 아님 |
| 최초 사용자 등록 메뉴 | 캡쳐 이미지 기준 요구사항이 아님 |
| 세션 ID 발급 중심 인증 설계 | 캡쳐 이미지에 세션 ID 요구가 없음 |
| 로그아웃 메뉴 | 캡쳐 이미지에 직접 명시되지 않음 |
| `key=value` payload 메시지 예시 | 캡쳐 이미지 기준보다 과도하게 상세한 기존 설계라 단순화 |
| `requirements.md` 참조 | 해당 파일은 이전 PDF 추출 기반 내용이 섞여 있어 이번 기준 자료에서 제외 |

## 5. 캡쳐 이미지 기준으로 유지한 요구사항

| 캡쳐 요구사항 | 반영 문서 |
| --- | --- |
| Windows PC에서 정상 동작하는 C 언어 콘솔 프로그램 | `요구사항분석서.md`, `개발계획서.md` |
| 학생정보 저장 및 관리 | `요구사항분석서.md`, `개발계획서.md` |
| 학생정보 항목: 이름, 학번, 소속학과, 학년, GPA, 이메일 연락처, 모바일 폰 연락처 | `요구사항분석서.md`, `개발계획서.md` |
| 학생정보 관리 서버 필요 | `요구사항분석서.md`, `개발계획서.md` |
| 클라이언트 로그인 후 인가된 사용자만 기능 사용 | `요구사항분석서.md`, `개발계획서.md` |
| 서버는 초기화 및 클라이언트 요청 수행 | `요구사항분석서.md`, `개발계획서.md` |
| 클라이언트는 사용자 명령 기반 요청 송신 및 결과 제공 | `요구사항분석서.md`, `개발계획서.md` |
| 로그인된 사용자만 신규 사용자 추가 가능 | `요구사항분석서.md`, `개발계획서.md` |
| 퇴사자 고려 사용자 삭제 | `요구사항분석서.md`, `개발계획서.md` |
| `users.txt` 사용자정보 탭 구분 관리 | `요구사항분석서.md`, `개발계획서.md` |
| `SMU_Students.txt` 학생정보 저장 및 01시 저장 | `요구사항분석서.md`, `개발계획서.md` |
| 학생정보 검색, 삭제, 추가, 정보변경 | `요구사항분석서.md`, `개발계획서.md` |
| 학과명을 통한 학과 소속 전체 학생정보 출력 | `요구사항분석서.md`, `개발계획서.md` |
| 9,000명 학생정보 연결 리스트 관리 | `요구사항분석서.md`, `개발계획서.md` |
| 적절한 메뉴와 인터페이스 제공 | `요구사항분석서.md`, `개발계획서.md` |
| 서버-클라이언트 송수신 메시지 포맷 정의 | `요구사항분석서.md`, `개발계획서.md` |
| 필요한 구조체 및 구조체 멤버 정의 | `요구사항분석서.md`, `개발계획서.md` |
| 요구사항 외 기능 추가 금지 | `요구사항분석서.md`, `개발계획서.md` |
| 계획서 내 필요 도식 추가 | `개발계획서.md` |

## 6. 추가 확인 결과

- `requirements.md`는 이번 요청의 수정 대상이 아니므로 변경하지 않았다.
- `ref.txt`에는 캡쳐 이미지에 보이지 않는 내용이 포함되어 있어, 문서 본문에는 캡쳐 기준 요구사항만 반영했다.
- 기타 사항 및 감점 사항의 3번째 항목은 사용자 지시에 따라 요구사항 분석서와 개발계획서에 반영하지 않았다.
- 최종 점검에서 제출 문서 본문에 남아 있던 `캡쳐 이미지`, `ref.txt` 같은 작업 메타 표현은 제거하고, `수정사항.md`에만 근거 기록으로 남겼다.
- 캡쳐 이미지에서 학생정보 검색의 세부 기준을 고정하지 않았으므로, 제출 문서의 검색 기준 표현을 `학번` 단정에서 `검색값`으로 완화했다.
