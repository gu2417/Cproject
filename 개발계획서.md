# C응용프로젝트 개발 계획서

## [대학 교직원을 위한 온라인 학생관리 시스템]

| 항목 | 내용 |
| --- | --- |
| 문서명 | 개발 계획서 |
| 프로젝트명 | 대학 교직원을 위한 온라인 학생관리 시스템 |
| 영문명 | Online Student Management System for University Faculty |
| 수행기간 | 2026.04.27 ~ 2026.04.30 |
| 개발언어 | C |
| 실행환경 | Windows PC 콘솔 환경 |
| 시스템 형태 | 서버/클라이언트 기반 콘솔 프로그램 |
| 기준 문서 | `requirements.md`, `요구사항분석서.md`, `reference/2026학년도_1학기_중간고사대체과제.pdf` |

## 1 프로젝트 개요

### 프로젝트 기본 정보

| 구분 | 내용 |
| --- | --- |
| 국문 프로젝트 명 | 대학 교직원을 위한 온라인 학생관리 시스템 |
| 영문 프로젝트 명 | Online Student Management System for University Faculty |
| 개발 목적 | 대학 교직원이 학생 정보를 인증 절차 후 안전하게 조회, 추가, 수정, 삭제할 수 있는 서버/클라이언트 콘솔 시스템 개발 |
| 주요 사용자 | 대학 교직원, 사용자 관리자, 서버 운영자 |
| 핵심 데이터 | 학생 정보, 사용자 계정 정보, 단과대학-학과 매핑 정보 |
| 저장 파일 | `SMU_Students.txt`, `users.txt` |
| 핵심 자료구조 | 학생 정보 연결 리스트, 사용자 정보 목록, `DAEHWAK` 단과대학 구조체 |

### 참여자

| 소속 | 성명 | 학년 | 학번 | 이메일 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 1-1. 프로젝트 개발의 필요성

### 학생 정보 관리 업무의 집중화 필요

- 선문대학교에는 약 9,000명의 학생이 재학 중인 것으로 가정한다.
- 학생 정보는 이름, 학번, 소속학과, 학년, GPA, 이메일, 모바일 연락처처럼 행정 업무에서 반복적으로 사용되는 핵심 정보를 포함한다.
- 교직원이 학생 정보를 개별 파일이나 수기 방식으로 관리하면 중복 입력, 누락, 수정 이력 혼선, 검색 지연이 발생할 수 있다.
- 서버 프로그램이 학생 정보를 연결 리스트로 통합 관리하고, 클라이언트 프로그램이 요청만 전송하는 구조를 사용하면 학생 정보 관리 지점을 서버로 집중시킬 수 있다.

### 인증 기반 접근 제어 필요

- 학생 정보는 개인정보 성격의 데이터를 포함하므로 아무 사용자나 접근할 수 없어야 한다.
- 시스템은 `users.txt`에 등록된 사용자 ID와 암호를 기준으로 로그인 절차를 수행해야 한다.
- 인가된 사용자만 학생 정보 검색, 추가, 수정, 삭제 기능을 사용할 수 있어야 한다.
- 신규 교직원 추가와 퇴사자 계정 삭제도 로그인된 사용자에게만 허용해야 한다.

### 서버/클라이언트 분리 필요

- 학생 정보는 서버가 일관되게 관리하고, 클라이언트는 사용자 입력과 결과 출력에 집중해야 한다.
- 서버/클라이언트 구조를 사용하면 학생 정보 파일 접근, 연결 리스트 관리, 저장 정책을 서버 한 곳에서 통제할 수 있다.
- 클라이언트는 사용자 명령을 메시지로 변환해 서버에 보내고, 서버 응답을 콘솔에 출력하는 역할로 단순화된다.

### 파일 기반 관리와 자동 저장 필요

- 기존 학생 정보는 `SMU_Students.txt`에 저장되어 있으며 서버 시작 시 로딩되어야 한다.
- 사용자 정보는 `users.txt`에서 관리되어야 한다.
- 학생 정보의 추가, 수정, 삭제 결과는 서버 메모리의 연결 리스트에 반영되고 저장 시점에 파일로 기록되어야 한다.
- 요구사항에 따라 서버는 매일 01시 00분에 업데이트, 추가, 삭제된 학생 정보를 `SMU_Students.txt`에 저장해야 한다.

## 1-2. 프로젝트의 목표

### 최종 목표

Windows PC에서 동작하는 C 언어 콘솔 기반 서버/클라이언트 학생관리 시스템을 개발한다. 서버는 학생 정보를 연결 리스트로 관리하고, 클라이언트는 로그인한 교직원이 메뉴를 통해 학생 정보와 사용자 계정을 관리할 수 있도록 한다.

### 세부 목표

| 구분 | 목표 |
| --- | --- |
| 서버 | 학생 정보 파일 로딩, 사용자 인증, 요청 라우팅, 연결 리스트 관리, 파일 저장 수행 |
| 클라이언트 | 로그인, 메뉴 제공, 입력 검증, 요청 메시지 생성, 응답 출력 수행 |
| 인증 | `users.txt` 기반 로그인, 최초 사용자 등록, 사용자 추가, 사용자 삭제 지원 |
| 학생 관리 | 학번 조회, 학과별 조회, 단과대학별 조회, 학생 추가, 학생 수정, 학생 삭제 지원 |
| 파일 관리 | `SMU_Students.txt`와 `users.txt`를 탭 구분 텍스트 파일로 관리 |
| 자료구조 | 학생 정보는 연결 리스트로 관리하고, 단과대학 정보는 `DAEHWAK` 구조체로 관리 |
| 메시지 | 서버/클라이언트 간 요청과 응답을 표준 `key=value` 블록 메시지로 정의 |
| 예외 처리 | 인증 오류, 입력 오류, 파일 오류, 중복 데이터, 검색 결과 없음 등을 표준 오류 코드로 처리 |

### 개발 결과물

- 서버 실행 파일: 학생 정보 관리, 인증, 파일 저장 담당
- 클라이언트 실행 파일: 사용자 메뉴, 입력, 서버 요청, 결과 출력 담당
- 데이터 파일: `SMU_Students.txt`, `users.txt`
- 공통 헤더: 구조체, 오류 코드, 메시지 명령 코드, 상수 정의
- 문서 산출물: 요구사항 분석서, 개발 계획서, 시스템 구성도, 메시지 포맷 정의, 구조체 정의

## 1-3. 프로젝트의 개발 개요

### 시스템 구성도

```text
┌──────────────────────────────────────────────────────────────┐
│                         Client Program                       │
│  ┌─────────────────┐  ┌─────────────────┐  ┌──────────────┐ │
│  │ 로그인/메뉴 UI  │  │ 입력값 검증기   │  │ 요청 생성기  │ │
│  └────────┬────────┘  └────────┬────────┘  └──────┬───────┘ │
│           │                    │                  │         │
│           └────────────────────┴──────────────────┘         │
│                              │                               │
│                     표준 요청 메시지 전송                    │
└──────────────────────────────┼───────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────┐
│                         Server Program                       │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────────┐ │
│  │ 메시지 파서  │  │ 인증 관리자  │  │ 요청 라우터        │ │
│  └──────┬───────┘  └──────┬───────┘  └─────────┬──────────┘ │
│         │                 │                    │            │
│         ▼                 ▼                    ▼            │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────────┐ │
│  │ 사용자 관리  │  │ 학생 CRUD    │  │ 학과/단과대학 검색 │ │
│  └──────┬───────┘  └──────┬───────┘  └─────────┬──────────┘ │
│         │                 │                    │            │
│         └─────────────────┴────────────────────┘            │
│                              │                               │
│                연결 리스트 / 파일 저장 관리자                │
└──────────────────────────────┼───────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────┐
│                         File System                          │
│  users.txt: 사용자ID, 암호, 소속부서, 이름                   │
│  SMU_Students.txt: 학번, 이름, 학과, 학년, GPA, 이메일, 연락처│
└──────────────────────────────────────────────────────────────┘
```

### 서버-클라이언트 처리 흐름도

```text
[클라이언트 실행]
      │
      ▼
[서버 연결 확인]
      │
      ▼
[사용자 ID/암호 입력]
      │
      ▼
[LOGIN 요청 메시지 생성] ───────▶ [서버 메시지 파싱]
                                      │
                                      ▼
                              [users.txt 기준 인증]
                                      │
                      ┌───────────────┴───────────────┐
                      ▼                               ▼
              [로그인 성공 응답]              [로그인 실패 응답]
                      │                               │
                      ▼                               ▼
              [메인 메뉴 출력]                [로그인 화면 복귀]
                      │
                      ▼
          [학생/사용자 관리 요청 전송]
                      │
                      ▼
              [서버 요청 처리]
                      │
                      ▼
              [응답 출력 및 메뉴 복귀]
```

### 학생 연결 리스트 구조도

```text
student_head
    │
    ▼
┌─────────────────────────────────────────────┐
│ Student Node                                │
│ student_id: 20260001                        │
│ name: 홍길동                                │
│ department: 컴퓨터공학과                    │
│ grade: 2                                    │
│ gpa: 4.1                                    │
│ email: student@example.com                  │
│ mobile: 010-0000-0000                       │
│ next ───────────────────────────────────────┼──▶ 다음 Student Node
└─────────────────────────────────────────────┘
```

### 파일 입출력 흐름도

```text
서버 시작
   │
   ├─ users.txt 읽기 ───────────────▶ 사용자 인증 데이터 준비
   │
   ├─ SMU_Students.txt 읽기 ───────▶ Student 연결 리스트 생성
   │
   ▼
클라이언트 요청 처리
   │
   ├─ 학생 추가/수정/삭제 발생
   │        │
   │        ▼
   │   변경 플래그 true
   │
   ├─ 매일 01:00 저장 조건 충족
   │        │
   │        ▼
   │   SMU_Students.tmp 저장 후 SMU_Students.txt 교체
   │
   └─ 서버 종료 시 미저장 변경 확인 후 저장
```

### 개발 범위

| 포함 기능 | 설명 |
| --- | --- |
| 로그인 | `users.txt`의 사용자 ID와 암호로 인증 |
| 최초 사용자 등록 | 유효 사용자가 0명일 때만 허용 |
| 사용자 추가 | 로그인된 사용자가 신규 교직원 계정 추가 |
| 사용자 삭제 | 로그인된 사용자가 퇴사자 계정 삭제 |
| 학생 단일 조회 | 학번 기준 학생 상세 정보 조회 |
| 학과별 조회 | 학과명 기준 전체 학생 목록 조회 |
| 단과대학별 조회 | `DAEHWAK` 구조체의 학과 목록 기준 학생 목록 조회 |
| 학생 추가 | 중복 학번 검증 후 연결 리스트에 학생 추가 |
| 학생 수정 | 학번 기준 대상 검색 후 수정 가능 필드 갱신 |
| 학생 삭제 | 학번 기준 대상 검색 후 연결 리스트에서 제거 |
| 학생 정보 저장 | 01시 자동 저장, 서버 종료 저장, 명시 저장 |

| 제외 기능 | 제외 사유 |
| --- | --- |
| GUI | 과제 요구사항은 콘솔 프로그램 |
| 데이터베이스 | 과제 요구사항은 파일 기반 관리 |
| 웹/모바일 앱 | 과제 요구 범위 외 |
| 성적 산출/장학금 계산 | 학생 정보 관리 범위를 벗어남 |
| 요구사항 외 통계 기능 | 요구사항 외 기능 추가 금지 조건 준수 |

## 1-4. 활용방안 및 기대효과

### 활용방안

- 교직원이 로그인 후 학생 정보를 학번, 학과명, 단과대학명 기준으로 조회한다.
- 신규 학생 정보가 발생하면 학생 추가 메뉴를 통해 서버의 연결 리스트에 반영한다.
- 학생의 학년, GPA, 이메일, 모바일 연락처가 바뀌면 학생 수정 메뉴를 통해 갱신한다.
- 제적, 졸업, 잘못 등록된 데이터 등 삭제가 필요한 경우 학생 삭제 메뉴를 통해 제거한다.
- 신규 직원은 로그인된 사용자가 사용자 추가 메뉴로 등록한다.
- 퇴사자는 사용자 삭제 메뉴로 `users.txt`에서 제거한다.
- 서버는 01시 자동 저장 또는 종료 시 저장으로 변경된 학생 정보를 파일에 반영한다.

### 기대효과

| 기대효과 | 상세 설명 |
| --- | --- |
| 업무 효율 향상 | 교직원이 콘솔 메뉴로 학생 정보를 빠르게 조회하고 관리할 수 있다. |
| 데이터 일관성 확보 | 학생 정보 변경이 서버의 연결 리스트와 파일 저장 정책을 통해 일관되게 관리된다. |
| 접근 통제 강화 | 로그인된 사용자만 학생 정보와 사용자 정보를 관리할 수 있다. |
| 파일 기반 단순 운영 | 별도 데이터베이스 없이 `txt` 파일만으로 과제 요구사항을 충족한다. |
| 구조적 확장성 | 서버, 클라이언트, 공통 모듈을 분리하여 기능별 구현과 테스트가 쉽다. |
| 요구사항 추적 용이 | 요구사항 분석서의 S/C/ETC 항목이 개발 모듈과 대응된다. |

## 2 프로젝트 개발 상세 계획

## 2-1. 주요 구성요소 상세 개발 계획

### □ 서버

서버는 데이터의 최종 관리 주체이다. 클라이언트 요청을 수신하고, 인증과 입력 검증을 수행한 뒤 학생 연결 리스트와 사용자 파일을 변경한다.

### 서버 구성요소 1. 서버 초기화 관리자

| 항목 | 내용 |
| --- | --- |
| 목적 | 서버 실행 직후 데이터 파일, 연결 리스트, 단과대학 목록, 세션 목록, 변경 플래그 초기화 |
| 입력 | `SMU_Students.txt`, `users.txt`, 단과대학 초기화 데이터 |
| 출력 | 초기화 성공/실패 상태, 로딩된 학생 수, 유효 사용자 수, 오류 레코드 수 |
| 주요 함수 후보 | `initServer()`, `initCollegeList()`, `loadStudentsFromFile()`, `loadUsersFromFile()` |
| 관련 요구사항 | S001, S002, S003, S013, S016 |

상세 처리 절차:

1. 학생 리스트 헤드 포인터를 `NULL`로 초기화한다.
2. 사용자 리스트 또는 사용자 파일 상태를 초기화한다.
3. 세션 리스트를 `NULL`로 초기화한다.
4. 학생 정보 변경 플래그를 `false`로 설정한다.
5. `SMU_Students.txt`를 읽어 학생 연결 리스트를 구성한다.
6. `users.txt`를 읽어 유효 사용자 수를 확인한다.
7. `DAEHWAK` 단과대학 목록을 초기화한다.
8. 초기화 실패 시 서버 요청 대기 상태로 진입하지 않는다.

### 서버 구성요소 2. 학생 파일 로더

| 항목 | 내용 |
| --- | --- |
| 목적 | `SMU_Students.txt`의 학생 레코드를 읽어 연결 리스트로 변환 |
| 파일 형식 | `학번<TAB>이름<TAB>소속학과<TAB>학년<TAB>GPA<TAB>이메일<TAB>모바일폰` |
| 정상 조건 | 필드 7개, 학번 중복 없음, 학년/GPA 변환 가능, 필수값 존재 |
| 오류 처리 | 빈 줄 무시, 필드 수 오류 기록, 중복 학번 기록, 숫자 변환 오류 기록 |
| 주요 함수 후보 | `parseStudentLine()`, `validateStudent()`, `createStudentNode()`, `appendStudentNode()` |

학생 레코드 검증 규칙:

| 필드 | 검증 |
| --- | --- |
| 학번 | 빈 값 불가, 중복 불가, 탭/줄바꿈 금지 |
| 이름 | 빈 값 불가 |
| 소속학과 | 빈 값 불가 |
| 학년 | 정수, 기본 1~4 |
| GPA | 실수, 기본 0.0~4.5 |
| 이메일 | 빈 값 불가, `@`와 `.` 포함 |
| 모바일폰 | 빈 값 불가, 숫자와 하이픈 중심 |

### 서버 구성요소 3. 사용자 파일 관리자

| 항목 | 내용 |
| --- | --- |
| 목적 | `users.txt` 기반 로그인 검증, 사용자 추가, 사용자 삭제 |
| 파일 형식 | `사용자ID<TAB>사용자암호<TAB>사용자소속부서<TAB>사용자이름` |
| 정상 조건 | 필드 4개, 사용자 ID 중복 없음, 모든 필수값 존재 |
| 주요 함수 후보 | `loadUsersFromFile()`, `findUserById()`, `appendUserToFile()`, `rewriteUsersFileExcept()` |
| 관련 요구사항 | S003, S004, S006, S007 |

사용자 추가 정책:

- 로그인된 사용자만 사용자 추가 가능
- 최초 사용자 등록은 유효 사용자가 0명일 때만 인증 없이 허용
- 사용자 ID 중복 시 실패
- 저장 성공까지 확인한 뒤 성공 응답 반환

사용자 삭제 정책:

- 로그인된 사용자만 사용자 삭제 가능
- 존재하지 않는 사용자 ID 삭제 불가
- 현재 로그인 사용자 삭제 제한
- 마지막 남은 사용자 삭제 제한
- 임시 파일에 먼저 기록 후 원본 파일 교체

### 서버 구성요소 4. 인증 및 세션 관리자

| 항목 | 내용 |
| --- | --- |
| 목적 | 로그인 인증, 세션 생성, 세션 검증, 로그아웃 처리 |
| 입력 | 사용자 ID, 암호, 세션 ID |
| 출력 | 로그인 성공/실패, 세션 ID, 인증 오류 코드 |
| 주요 함수 후보 | `authenticateUser()`, `createSession()`, `validateSession()`, `removeSession()` |
| 관련 오류 | `ERR_INVALID_LOGIN`, `ERR_AUTH_REQUIRED`, `ERR_INVALID_SESSION` |

인증 흐름:

```text
LOGIN 요청 수신
   │
   ├─ user_id/password 필수값 검증
   │
   ├─ users.txt 또는 사용자 목록에서 user_id 검색
   │
   ├─ 암호 비교
   │
   ├─ 성공: 세션 ID 생성 후 SUCCESS 응답
   │
   └─ 실패: ERR_INVALID_LOGIN 응답
```

### 서버 구성요소 5. 메시지 파서 및 생성기

| 항목 | 내용 |
| --- | --- |
| 목적 | 클라이언트 요청 메시지를 파싱하고 서버 응답 메시지를 생성 |
| 요청 필드 | `type`, `command`, `request_id`, `session_id`, `payload_length`, `payload`, `end` |
| 응답 필드 | `type`, `command`, `request_id`, `status`, `error_code`, `message`, `payload_length`, `payload`, `end` |
| 주요 함수 후보 | `parseRequestMessage()`, `parsePayload()`, `buildSuccessResponse()`, `buildFailResponse()` |
| 관련 요구사항 | S015 |

요청 메시지 예:

```text
type=REQ
command=STUDENT_GET
request_id=REQ-0002
session_id=SID-20260427-0001
payload_length=19
payload=student_id=20260001
end=MSG
```

응답 메시지 예:

```text
type=RES
command=STUDENT_GET
request_id=REQ-0002
status=SUCCESS
error_code=NONE
message=student_found
payload_length=130
payload=student_id=20260001&name=HongGilDong&department=ComputerEngineering&grade=2&gpa=4.1&email=student@example.com&mobile=010-0000-0000
end=MSG
```

### 서버 구성요소 6. 요청 라우터

| 항목 | 내용 |
| --- | --- |
| 목적 | `command` 값에 따라 적절한 업무 처리 함수 호출 |
| 입력 | 파싱된 요청 메시지 |
| 출력 | 표준 응답 메시지 |
| 주요 함수 후보 | `routeRequest()`, `isAuthRequiredCommand()`, `dispatchStudentCommand()`, `dispatchUserCommand()` |

명령 코드별 처리 함수:

| 명령 코드 | 처리 함수 후보 |
| --- | --- |
| `LOGIN` | `handleLogin()` |
| `FIRST_USER_REGISTER` | `handleFirstUserRegister()` |
| `USER_ADD` | `handleUserAdd()` |
| `USER_DELETE` | `handleUserDelete()` |
| `STUDENT_GET` | `handleStudentGet()` |
| `STUDENT_ADD` | `handleStudentAdd()` |
| `STUDENT_UPDATE` | `handleStudentUpdate()` |
| `STUDENT_DELETE` | `handleStudentDelete()` |
| `DEPARTMENT_LIST` | `handleDepartmentList()` |
| `COLLEGE_LIST` | `handleCollegeList()` |
| `SAVE` | `handleSave()` |
| `LOGOUT` | `handleLogout()` |

### 서버 구성요소 7. 학생 연결 리스트 관리자

| 항목 | 내용 |
| --- | --- |
| 목적 | 학생 데이터의 추가, 검색, 수정, 삭제, 전체 해제 |
| 자료구조 | 단방향 연결 리스트 |
| 주요 함수 후보 | `findStudentById()`, `appendStudentNode()`, `updateStudent()`, `deleteStudentById()`, `freeStudentList()` |
| 관련 요구사항 | S008, S009, S010, S011, S012, S013, S016 |

연결 리스트 처리 정책:

- 추가 시 중복 학번 검사를 먼저 수행한다.
- 수정 시 대상 학번이 존재해야 한다.
- 삭제 시 첫 노드, 중간 노드, 마지막 노드 삭제를 모두 처리한다.
- 삭제된 노드는 반드시 메모리 해제한다.
- 서버 종료 시 전체 리스트를 해제한다.

### 서버 구성요소 8. 학생 조회 및 CRUD 관리자

| 기능 | 처리 계획 |
| --- | --- |
| 학생 단일 조회 | 학번으로 연결 리스트 순회, 일치 노드 반환 |
| 학생 추가 | 입력 검증, 중복 검사, 노드 생성, 리스트 삽입, 변경 플래그 설정 |
| 학생 수정 | 대상 검색, 변경 필드 검증, 기존 노드 갱신, 변경 플래그 설정 |
| 학생 삭제 | 대상 검색, 리스트 분리, 메모리 해제, 변경 플래그 설정 |

학생 수정 가능 필드:

- 이름
- 소속학과
- 학년
- GPA
- 이메일
- 모바일폰

학번은 기본적으로 수정하지 않는다. 학번 변경을 허용할 경우 중복 검사가 필요하지만, 본 계획에서는 요구사항을 단순하고 안정적으로 만족하기 위해 학번 변경을 제외한다.

### 서버 구성요소 9. 학과 및 단과대학 검색 관리자

| 항목 | 내용 |
| --- | --- |
| 목적 | 학과명 또는 단과대학명으로 학생 목록 조회 |
| 학과 검색 | 학생의 `department` 필드와 입력 학과명 정확 일치 비교 |
| 단과대학 검색 | `DAEHWAK.departments` 목록과 학생의 `department` 비교 |
| 주요 함수 후보 | `findStudentsByDepartment()`, `findCollegeByName()`, `findStudentsByCollege()` |

단과대학 구조체 설계:

```c
typedef struct DAEHWAK {
    char college_name[100];
    char departments[50][100];
    int department_count;
    struct DAEHWAK *next;
} DAEHWAK;
```

검색 흐름:

```text
COLLEGE_LIST 요청
   │
   ├─ college 필수값 검증
   ├─ DAEHWAK 목록에서 단과대학명 검색
   ├─ 단과대학 없음: ERR_COLLEGE_NOT_FOUND
   ├─ 단과대학 있음: 소속학과 목록 확보
   ├─ 학생 연결 리스트 전체 순회
   ├─ 학생 department가 소속학과 목록에 포함되면 결과 추가
   └─ 결과 반환 또는 ERR_NO_RESULT
```

### 서버 구성요소 10. 파일 저장 관리자

| 항목 | 내용 |
| --- | --- |
| 목적 | 변경된 학생 연결 리스트를 `SMU_Students.txt`에 저장 |
| 저장 시점 | 매일 01시 00분, 서버 종료 시, `SAVE` 명령 수신 시 |
| 저장 방식 | 임시 파일 저장 후 원본 교체 |
| 주요 함수 후보 | `saveStudentsToFile()`, `shouldAutoSave()`, `replaceStudentFile()` |

저장 정책:

- 학생 추가, 수정, 삭제 성공 시 `student_dirty = true`로 설정한다.
- 저장 성공 시 `student_dirty = false`로 변경한다.
- 저장 실패 시 `student_dirty`를 유지한다.
- 자동 저장은 같은 날짜에 중복 실행되지 않도록 마지막 저장 날짜를 기록한다.

### □ 클라이언트

클라이언트는 사용자의 입력을 받고 서버에 표준 요청 메시지를 전송하며, 서버 응답을 사용자에게 이해 가능한 형태로 출력한다.

### 클라이언트 구성요소 1. 서버 연결 관리자

| 항목 | 내용 |
| --- | --- |
| 목적 | 서버 실행 여부 확인, 요청 송신, 응답 수신, 연결 종료 |
| 주요 함수 후보 | `connectToServer()`, `sendRequest()`, `receiveResponse()`, `closeConnection()` |
| 오류 처리 | 서버 연결 실패 시 재시도 또는 종료 선택 |

### 클라이언트 구성요소 2. 로그인 전 화면

| 메뉴 | 설명 |
| --- | --- |
| 로그인 | 사용자 ID와 암호를 입력받아 `LOGIN` 요청 전송 |
| 최초 사용자 등록 | 서버가 허용할 때만 `FIRST_USER_REGISTER` 요청 전송 |
| 종료 | 클라이언트 종료 |

로그인 성공 시 서버가 반환한 `session_id`를 저장하고 메인 메뉴로 이동한다. 로그인 실패 시 로그인 전 화면으로 돌아간다.

### 클라이언트 구성요소 3. 로그인 후 메인 메뉴

| 번호 | 메뉴 | 서버 명령 |
| --- | --- | --- |
| 1 | 학생 단일 조회 | `STUDENT_GET` |
| 2 | 학과별 학생 조회 | `DEPARTMENT_LIST` |
| 3 | 단과대학별 학생 조회 | `COLLEGE_LIST` |
| 4 | 학생 추가 | `STUDENT_ADD` |
| 5 | 학생 수정 | `STUDENT_UPDATE` |
| 6 | 학생 삭제 | `STUDENT_DELETE` |
| 7 | 사용자 추가 | `USER_ADD` |
| 8 | 사용자 삭제 | `USER_DELETE` |
| 9 | 로그아웃 | `LOGOUT` |
| 0 | 종료 | 연결 종료 |

### 클라이언트 구성요소 4. 입력값 검증기

| 입력값 | 검증 |
| --- | --- |
| 메뉴 번호 | 숫자 변환 가능, 허용 범위 내 |
| 학번 | 빈 값 불가, 탭/줄바꿈 금지 |
| 이름 | 빈 값 불가 |
| 학과명 | 빈 값 불가 |
| 학년 | 정수 변환 가능, 기본 1~4 |
| GPA | 실수 변환 가능, 기본 0.0~4.5 |
| 이메일 | 빈 값 불가, `@`와 `.` 포함 |
| 모바일폰 | 빈 값 불가 |
| 사용자 ID | 빈 값 불가, 탭/줄바꿈 금지 |
| 암호 | 빈 값 불가, 화면 재출력 금지 |

입력은 `fgets` 기반 한 줄 입력으로 받고, 숫자 변환은 `strtol`, `strtod`를 사용한다.

### 클라이언트 구성요소 5. 학생 관리 화면

학생 조회:

1. 학번 입력
2. `STUDENT_GET` 요청 생성
3. 서버 응답 수신
4. 성공 시 학생 상세 정보 출력
5. 실패 시 오류 메시지 출력

학생 추가:

1. 학번, 이름, 소속학과, 학년, GPA, 이메일, 모바일폰 입력
2. 클라이언트 1차 검증
3. 입력 요약 출력
4. 사용자 확인 후 `STUDENT_ADD` 요청 전송
5. 성공/실패 결과 출력

학생 수정:

1. 수정 대상 학번 입력
2. 대상 학생 조회
3. 기존 정보 출력
4. 수정 항목 선택
5. 변경값 입력
6. `STUDENT_UPDATE` 요청 전송
7. 수정 결과 출력

학생 삭제:

1. 삭제 대상 학번 입력
2. 대상 학생 조회
3. 대상 정보 출력
4. `Y` 입력 시에만 삭제 요청 전송
5. 삭제 결과 출력

### 클라이언트 구성요소 6. 사용자 관리 화면

사용자 추가:

- 사용자 ID, 암호, 소속부서, 이름 입력
- 암호는 확인 화면에 출력하지 않음
- `USER_ADD` 요청 전송
- 중복 ID, 파일 저장 실패, 인증 오류를 메시지로 출력

사용자 삭제:

- 삭제 대상 사용자 ID 입력
- 삭제 확인 후 `USER_DELETE` 요청 전송
- 마지막 사용자 삭제 제한, 현재 사용자 삭제 제한, 사용자 없음 오류를 메시지로 출력

### 클라이언트 구성요소 7. 응답 처리기

| 응답 상태 | 처리 |
| --- | --- |
| `SUCCESS` | 기능별 성공 메시지와 payload 데이터 출력 |
| `FAIL` + `ERR_AUTH_REQUIRED` | 세션 삭제 후 로그인 화면 이동 |
| `FAIL` + `ERR_INVALID_SESSION` | 세션 삭제 후 로그인 화면 이동 |
| `FAIL` + `ERR_INVALID_INPUT` | 입력 오류 안내 후 재입력 |
| `FAIL` + `ERR_STUDENT_NOT_FOUND` | 학생 없음 메시지 출력 |
| `FAIL` + `ERR_DUPLICATE_STUDENT_ID` | 중복 학번 안내 |
| `FAIL` + `ERR_FILE_WRITE` | 저장 실패 안내 |

## 2-2. 프로젝트 추진 계획

### 개발 방법 및 추진전략

| 전략 | 상세 계획 |
| --- | --- |
| 요구사항 우선 구현 | 요구사항 분석서의 S/C/ETC 항목을 모듈과 함수로 매핑한다. |
| 서버 우선 개발 | 서버 초기화, 파일 로딩, 연결 리스트, 인증을 먼저 구현한다. |
| 클라이언트 단계 개발 | 로그인 화면, 메뉴, 학생 관리 화면, 사용자 관리 화면 순서로 구현한다. |
| 공통 모듈 분리 | 구조체, 오류 코드, 명령 코드, 메시지 포맷은 공통 헤더로 분리한다. |
| 파일 안정성 확보 | 저장 시 임시 파일을 사용해 원본 파일 손상을 줄인다. |
| 예외 처리 강화 | 파일 없음, 입력 오류, 중복 데이터, 인증 실패, 저장 실패를 모두 명시 처리한다. |
| 범위 통제 | 요구사항에 없는 통계, GUI, DB, 웹 기능은 개발하지 않는다. |

### 개발 모듈 구성안

```text
project/
├─ include/
│  ├─ common.h          # 공통 상수, 오류 코드, 명령 코드
│  ├─ student.h         # Student 구조체와 학생 함수 선언
│  ├─ user.h            # User 구조체와 사용자 함수 선언
│  ├─ college.h         # DAEHWAK 구조체와 단과대학 함수 선언
│  ├─ message.h         # 요청/응답 메시지 구조체와 파서 선언
│  └─ session.h         # Session 구조체와 인증 함수 선언
├─ server/
│  ├─ server_main.c
│  ├─ server_init.c
│  ├─ request_router.c
│  ├─ auth_manager.c
│  ├─ student_manager.c
│  ├─ user_manager.c
│  ├─ college_manager.c
│  ├─ file_storage.c
│  └─ message_handler.c
├─ client/
│  ├─ client_main.c
│  ├─ menu.c
│  ├─ input_validator.c
│  ├─ request_builder.c
│  └─ response_viewer.c
├─ data/
│  ├─ users.txt
│  └─ SMU_Students.txt
└─ docs/
   ├─ requirements.md
   ├─ 요구사항분석서.md
   └─ 개발계획서.md
```

실제 과제 제출 구조가 별도로 지정될 경우 위 구조를 간소화할 수 있다. 단, 서버/클라이언트/공통 정의/데이터 파일의 역할 분리는 유지한다.

### 담당 역할

개인 프로젝트 기준으로 작성한다. 팀 프로젝트로 수행할 경우 담당자를 팀원명으로 변경한다.

| 순번 | 팀원명 | 담당 역할 | 비고 |
| --- | --- | --- | --- |
| 1 | 작성자 | 요구사항 분석, 개발계획서 작성, 서버 초기화, 파일 로딩 | 문서 및 서버 기반 |
| 2 | 작성자 | 인증, 사용자 관리, 세션 관리, 메시지 포맷 구현 | 서버 기능 |
| 3 | 작성자 | 학생 연결 리스트, CRUD, 학과/단과대학 검색 구현 | 핵심 기능 |
| 4 | 작성자 | 클라이언트 메뉴, 입력 검증, 응답 출력 구현 | 클라이언트 기능 |
| 5 | 작성자 | 테스트, 오류 처리, 파일 저장 검증, 최종 정리 | 검증 |

### 프로젝트 추진 상세 일정

| 주요 내용 | 담당자 | 1일 | 2일 | 3일 | 4일 | 5일 | 6일 | 7일 | 8일 | 9일 | 10일 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 과제 요구사항 분석 및 범위 확정 | 작성자 | ● |  |  |  |  |  |  |  |  |  |
| 요구사항 분석서 및 개발계획서 작성 | 작성자 | ● | ● |  |  |  |  |  |  |  |  |
| 공통 구조체, 오류 코드, 명령 코드 정의 | 작성자 |  | ● | ● |  |  |  |  |  |  |  |
| 서버 초기화 및 학생 파일 로딩 구현 | 작성자 |  |  | ● | ● |  |  |  |  |  |  |
| 사용자 파일 로딩 및 로그인 인증 구현 | 작성자 |  |  | ● | ● |  |  |  |  |  |  |
| 메시지 파서, 응답 생성기, 요청 라우터 구현 | 작성자 |  |  |  | ● | ● |  |  |  |  |  |
| 학생 조회, 추가, 수정, 삭제 구현 | 작성자 |  |  |  |  | ● | ● |  |  |  |  |
| 학과별 및 단과대학별 검색 구현 | 작성자 |  |  |  |  |  | ● | ● |  |  |  |
| 사용자 추가 및 삭제 구현 | 작성자 |  |  |  |  |  |  | ● | ● |  |  |
| 클라이언트 메뉴와 입력 검증 구현 | 작성자 |  |  |  |  | ● | ● | ● | ● |  |  |
| 파일 저장, 01시 자동 저장, 종료 저장 구현 | 작성자 |  |  |  |  |  |  | ● | ● |  |  |
| 통합 테스트 및 오류 처리 보강 | 작성자 |  |  |  |  |  |  |  | ● | ● |  |
| 최종 문서 정리 및 제출 준비 | 작성자 |  |  |  |  |  |  |  |  | ● | ● |

### 단계별 산출물

| 단계 | 산출물 |
| --- | --- |
| 분석 단계 | 요구사항 분석서, 개발계획서, 기능 목록, 메시지 포맷 |
| 설계 단계 | 구조체 정의, 모듈 구성도, 데이터 파일 형식, 오류 코드 |
| 구현 단계 | 서버 프로그램, 클라이언트 프로그램, 공통 헤더, 파일 입출력 함수 |
| 검증 단계 | 로그인 테스트, 학생 CRUD 테스트, 사용자 관리 테스트, 저장 테스트 |
| 제출 단계 | 최종 소스코드, 데이터 파일 예시, 요구사항 분석서, 개발계획서 |

## 3 상세 설계

### 3-1. 핵심 구조체 정의

```c
typedef struct Student {
    char student_id[20];
    char name[50];
    char department[100];
    int grade;
    double gpa;
    char email[100];
    char mobile[30];
    struct Student *next;
} Student;

typedef struct User {
    char user_id[50];
    char password[100];
    char department[100];
    char name[50];
    struct User *next;
} User;

typedef struct DAEHWAK {
    char college_name[100];
    char departments[50][100];
    int department_count;
    struct DAEHWAK *next;
} DAEHWAK;

typedef struct Session {
    char session_id[64];
    char user_id[50];
    int active;
    struct Session *next;
} Session;
```

### 3-2. 오류 코드 정의

| 오류 코드 | 의미 |
| --- | --- |
| `NONE` | 오류 없음 |
| `ERR_INVALID_LOGIN` | 사용자 ID 또는 암호 불일치 |
| `ERR_AUTH_REQUIRED` | 로그인 필요 |
| `ERR_INVALID_SESSION` | 유효하지 않은 세션 |
| `ERR_INVALID_MESSAGE` | 메시지 형식 오류 |
| `ERR_INVALID_INPUT` | 입력값 오류 |
| `ERR_FILE_READ` | 파일 읽기 실패 |
| `ERR_FILE_WRITE` | 파일 쓰기 실패 |
| `ERR_DUPLICATE_USER_ID` | 사용자 ID 중복 |
| `ERR_USER_NOT_FOUND` | 사용자 없음 |
| `ERR_DELETE_SELF_NOT_ALLOWED` | 현재 로그인 사용자 삭제 제한 |
| `ERR_LAST_USER_NOT_ALLOWED` | 마지막 사용자 삭제 제한 |
| `ERR_DUPLICATE_STUDENT_ID` | 학번 중복 |
| `ERR_STUDENT_NOT_FOUND` | 학생 없음 |
| `ERR_NO_RESULT` | 검색 결과 없음 |
| `ERR_COLLEGE_NOT_FOUND` | 단과대학 없음 |
| `ERR_MEMORY_ALLOC` | 메모리 할당 실패 |
| `ERR_SERVER_INTERNAL` | 기타 서버 내부 오류 |

### 3-3. 메시지 명령 코드 정의

| 명령 코드 | 설명 |
| --- | --- |
| `LOGIN` | 로그인 |
| `FIRST_USER_REGISTER` | 최초 사용자 등록 |
| `LOGOUT` | 로그아웃 |
| `USER_ADD` | 사용자 추가 |
| `USER_DELETE` | 사용자 삭제 |
| `STUDENT_GET` | 학생 단일 조회 |
| `STUDENT_ADD` | 학생 추가 |
| `STUDENT_UPDATE` | 학생 수정 |
| `STUDENT_DELETE` | 학생 삭제 |
| `DEPARTMENT_LIST` | 학과별 학생 조회 |
| `COLLEGE_LIST` | 단과대학별 학생 조회 |
| `SAVE` | 학생 정보 파일 저장 |

### 3-4. 데이터 파일 형식

`users.txt`

```text
사용자ID<TAB>사용자암호<TAB>사용자소속부서<TAB>사용자이름
```

예시:

```text
staff01	pass1234	교무처	김직원
```

`SMU_Students.txt`

```text
학번<TAB>이름<TAB>소속학과<TAB>학년<TAB>GPA<TAB>이메일<TAB>모바일폰
```

예시:

```text
20260001	홍길동	컴퓨터공학과	2	4.1	student@example.com	010-0000-0000
```

### 3-5. 주요 테스트 계획

| 테스트 ID | 테스트 내용 | 기대 결과 |
| --- | --- | --- |
| T001 | 올바른 ID와 암호로 로그인 | 세션 ID 발급 및 메인 메뉴 출력 |
| T002 | 잘못된 암호로 로그인 | 로그인 실패 메시지 출력 |
| T003 | 미로그인 상태에서 학생 조회 요청 | 인증 필요 오류 |
| T004 | 존재하는 학번 조회 | 학생 상세 정보 출력 |
| T005 | 존재하지 않는 학번 조회 | 학생 없음 메시지 출력 |
| T006 | 중복 없는 학생 추가 | 연결 리스트에 노드 추가 |
| T007 | 중복 학번 학생 추가 | 추가 실패 |
| T008 | 학생 정보 수정 | 수정값 반영 |
| T009 | 학생 삭제 | 삭제 후 조회 실패 |
| T010 | 학과별 조회 | 해당 학과 학생 목록 출력 |
| T011 | 단과대학별 조회 | 해당 단과대학 소속 학생 목록 출력 |
| T012 | 사용자 추가 | `users.txt`에 사용자 반영 |
| T013 | 사용자 삭제 | `users.txt`에서 사용자 제거 |
| T014 | 마지막 사용자 삭제 | 삭제 제한 오류 |
| T015 | 학생 정보 저장 | `SMU_Students.txt`에 변경 반영 |
| T016 | 잘못된 파일 형식 로딩 | 오류 레코드 분류 후 정상 레코드 처리 |

## References

1. `reference/2026학년도_1학기_중간고사대체과제.pdf`
2. `reference/[중간고사용]요구사항분석서.hwp.pdf`
3. `reference/[참고용]개발계획서.hwp.pdf`
4. `requirements.md`
5. `요구사항분석서.md`
