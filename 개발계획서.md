# 대학 교직원을 위한 온라인 학생관리 시스템 개발 계획서

## 1 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 (국문) | 대학 교직원을 위한 온라인 학생관리 시스템 |
| 프로젝트명 (영문) | Online Student Management System for University Faculty |
| 수행기간 | 2026. 04. 13 ~ 2026. 06. 30 |

### 참여자

| 번호 | 이름 | 학과 | 역할 |
|------|------|------|------|
| 1 | (작성) | 컴퓨터공학부 | (작성) |
| 2 | (작성) | 컴퓨터공학부 | (작성) |
| 3 | (작성) | 컴퓨터공학부 | (작성) |

### 1-1. 프로젝트 개발의 필요성

• **대학 교직원을 위한 학생관리 체계의 필요성**
  - 대학교에 재학 중인 약 9,000명의 학생 정보를 효율적으로 관리할 필요성이 있음
  - 기존 수동 관리 방식에서 체계적인 디지털 관리 시스템 요구
  - 교직원이 손쉽게 접근 가능한 콘솔 기반 프로그램 필요

• **서버-클라이언트 구조를 통한 신뢰성 있는 정보 관리**
  - 중앙 집중식 서버에서 학생 정보를 관리하고 인증된 사용자만 접근 가능하도록 구조화
  - 클라이언트 프로그램을 통해 사용자 명령에 기반한 학생정보 관리 기능 제공
  - 사용자 인증을 통해 권한 있는 교직원만 시스템 접근 가능하도록 보안성 강화

• **연결리스트 기반의 효율적 메모리 관리**
  - 9,000명 규모의 학생 정보를 동적으로 관리하기 위해 연결리스트 자료구조 활용
  - 학생 정보의 추가, 삭제, 검색 시 메모리 효율성 극대화

• **파일 기반 영속적 데이터 관리**
  - 기존 학생 정보는 "SMU_Students.txt" 파일에 저장
  - 매일 정해진 시간(01시 00분)에 업데이트된 학생 정보를 파일에 저장하여 영속성 보장
  - 사용자 정보는 "users.txt" 파일에서 관리

### 1-2. 프로젝트의 목표

• **C 언어 기반 콘솔 학생관리 시스템 개발**
  - Windows 운영체제에서 정상적으로 동작하는 C 언어 콘솔 프로그램 개발
  - 서버 1식 + 클라이언트 N식 구조로 구현
  - "DAEHWAK" 구조체를 기반으로 단과대학 정보 관리

• **핵심 기능 구현**
  - P0 (MVP): 사용자 로그인, 학생 정보 조회(검색/목록), 단과대학별 학생 조회
  - P1 (필수): 학생 정보 추가/삭제, 정보 수정
  - P2 (향상): 다양한 검색 필터(학년별, 학과별, GPA 범위), 통계 기능

• **사용자 편의성 제공**
  - 직관적인 메뉴 인터페이스로 쉬운 접근성 보장
  - 사용자 입력에 기반한 명령 처리 시스템 구현
  - 회원가입 기능으로 신규 교직원 추가 가능

• **안정적인 데이터 관리**
  - 탭(tab) 구분자를 사용한 구조화된 파일 형식 관리
  - 매일 정시에 자동 업데이트 기능 구현
  - 데이터 무결성 보장을 위한 입력 검증

### 1-3. 프로젝트의 개발 개요

#### 시스템 구성도

```
┌──────────────────────────────────────────────────────────────┐
│           클라이언트 (Student Management Client)              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  • 로그인 화면                                          │  │
│  │  • 학생 정보 검색 화면                                  │  │
│  │  • 학생 정보 추가/수정/삭제 화면                        │  │
│  │  • 단과대학별 조회 화면                                 │  │
│  │  • 메뉴 및 사용자 인터페이스                            │  │
│  └────────────────────────────────────────────────────────┘  │
│              메인 실행 루프 (사용자 입력 처리)                │
└─────────────────────────┬─────────────────────────────────────┘
                          │ TCP/IP Socket Communication
┌─────────────────────────▼─────────────────────────────────────┐
│           서버 (Student Management Server)                     │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  • 클라이언트 접속 관리                                 │  │
│  │  • 사용자 인증 처리 (users.txt)                         │  │
│  │  • 학생 정보 관리 (연결리스트 + 파일 I/O)              │  │
│  │  • 요청 라우팅 및 처리                                  │  │
│  │  • 응답 송신                                            │  │
│  └────────────────────────────────────────────────────────┘  │
└─────────────────────────┬─────────────────────────────────────┘
                          │ File I/O (TAB-delimited)
┌─────────────────────────▼─────────────────────────────────────┐
│           데이터 저장소 (File System)                           │
│  • users.txt (사용자 정보)                                     │
│  • SMU_Students.txt (학생 정보)                                │
└──────────────────────────────────────────────────────────────┘
```

#### 주요 구성 요소

| 컴포넌트 | 설명 |
|---------|------|
| chat_server (server.c) | 클라이언트 접속 관리, 요청 처리, 응답 송신, 데이터 파일 관리 |
| chat_client (client.c) | 사용자 입력 처리, 서버 통신, 메뉴 인터페이스 제공 |
| users.txt | 사용자 정보 저장소 (ID\tPassword\tDepartment\tName) |
| SMU_Students.txt | 학생 정보 저장소 (Name\tStudentID\tDepartment\tGrade\tGPA\tEmail\tPhone) |

#### 서버 처리 계층

```
네트워크 계층     : main.c (listen loop) → client_handler.c (request 수신)
요청 처리 계층    : request_router.c (패킷 타입별 핸들러 호출)
기능 계층         : auth.c / student_search.c / student_add.c / student_delete.c / student_update.c
데이터 관리 계층  : student_list.c (연결리스트) / file_manager.c (파일 I/O)
```

#### 프로토콜 정의

**패킷 형식**: `TYPE|field1|field2|...|content\n`

| 메시지 유형 | 설명 | 예시 |
|----------|------|------|
| LOGIN | 로그인 요청 | LOGIN\|alice\|password123 |
| LOGIN_RES | 로그인 응답 | LOGIN_RES\|0 (성공) / LOGIN_RES\|1 (ID오류) |
| STUDENT_SEARCH | 학생 검색 | STUDENT_SEARCH\|name / STUDENT_SEARCH\|grade\|department |
| STUDENT_SEARCH_RES | 검색 결과 | STUDENT_SEARCH_RES\|count\|data1;data2;... |
| STUDENT_ADD | 학생 추가 | STUDENT_ADD\|name\|id\|dept\|grade\|gpa\|email\|phone |
| STUDENT_DELETE | 학생 삭제 | STUDENT_DELETE\|student_id |
| STUDENT_UPDATE | 학생 정보 수정 | STUDENT_UPDATE\|student_id\|field\|new_value |
| STUDENT_LIST_BY_DEPT | 학과별 조회 | STUDENT_LIST_BY_DEPT\|department_name |

### 1-4. 활용방안 및 기대효과

• **교내 학생관리 시스템으로 즉시 활용**
  - 각 단과대학에서 학생 정보 조회 및 관리 도구로 활용
  - 교직원의 행정 업무 효율성 극대화

• **데이터 기반 의사결정 지원**
  - 학점, 학년별 학생 현황 분석 기능 제공
  - 교무처의 정책 수립을 위한 기초 데이터 제공

• **C 언어 네트워크 프로그래밍 학습 모델로 활용**
  - TCP 소켓 통신, 서버-클라이언트 구조, 동시 접속 처리 기법 습득
  - 연결리스트 등 자료구조와 파일 I/O 실무 학습

• **확장성과 유지보수성**
  - 연결리스트 기반 설계로 데이터 확장 용이
  - 모듈화된 구조로 기능 추가 및 수정 간편
  - 향후 데이터베이스 연동 시스템으로 업그레이드 가능

---

## 2 프로젝트 개발 상세 계획

### 2-1. 주요 구성요소 상세 개발 계획

#### □ 서버 (server.c, client_handler.c, etc.)

##### ▪ 메시지 프로토콜 및 패킷 생성

텍스트 기반의 메시지 프로토콜을 정의하여 클라이언트와 서버 간 통신을 수행한다.

• **패킷 구조**: `TYPE|field1|field2|...|content\n` (최대 4096 byte, UTF-8)

• **패킷 생성/파싱 함수** (protocol.c/h)
```c
/* 패킷 직렬화 함수 */
int build_packet(char *buf, size_t buflen, const char *type, ...);

/* 패킷 파싱 함수 */
int parse_packet(const char *line, char *type, char fields[][256], int max_fields);

/* 예: 학생 추가 패킷 생성 */
build_packet(buf, sizeof(buf), "STUDENT_ADD", "김철수", "2024001", 
             "컴퓨터공학부", "2", "3.85", "kim@email.com", "010-1234-5678", NULL);
/* → "STUDENT_ADD|김철수|2024001|컴퓨터공학부|2|3.85|kim@email.com|010-1234-5678\n" */
```

##### ▪ 사용자 인증 (auth.c/h)

클라이언트의 로그인 요청을 처리하여 사용자 인증을 수행한다.

• **사용자 정보 구조체** (types.h)
```c
typedef struct {
    char   user_id[21];        /* 사용자 ID (≤20자, ASCII) */
    char   password[33];       /* 사용자 암호 (≤32자) */
    char   department[51];     /* 소속 부서 (≤50자) */
    char   name[51];           /* 사용자 이름 (≤50자) */
    int    login_status;       /* 0=오프라인, 1=온라인 */
    int    socket_fd;          /* 접속 중 소켓 FD (-1=오프라인) */
} User;
```

• **인증 결과 코드**
  - 결과값 `0`: 인증 성공 → 세션 등록, 온라인 상태 변경
  - 결과값 `1`: 사용자 ID 불일치
  - 결과값 `2`: 사용자 Password 불일치
  - 결과값 `3`: 이미 접속 중인 계정

• **users.txt 파일 형식**: `UserID\tPassword\tDepartment\tName\n`
  - 탭 구분자로 필드 분리
  - 인증 시마다 파일에서 읽어 검증

##### ▪ 학생 정보 관리 (student_list.c/h)

연결리스트를 이용하여 9,000명의 학생 정보를 메모리에 로드하고 관리한다.

• **학생 정보 구조체** (types.h)
```c
typedef struct DAEHWAK {
    char   student_name[51];      /* 학생 이름 (≤50자) */
    char   student_id[21];        /* 학번 (≤20자) */
    char   department[51];        /* 소속학과 (≤50자) */
    int    grade;                 /* 학년 (1~4) */
    float  gpa;                   /* 평균학점 (0.0~4.5) */
    char   email[101];            /* 이메일 (≤100자) */
    char   phone[21];             /* 모바일 폰 (≤20자) */
    struct DAEHWAK *next;         /* 연결리스트 다음 노드 */
} DAEHWAK;
```

• **연결리스트 기본 연산**
  - `insert_student()`: 새로운 학생 정보 추가
  - `delete_student()`: 학생 정보 삭제
  - `search_student()`: 학번 또는 이름으로 검색
  - `search_by_department()`: 학과명으로 전체 학생 조회
  - `update_student()`: 학생 정보 수정

##### ▪ 파일 관리 (file_manager.c/h)

학생 정보를 파일에서 읽고 쓴다. 매일 01:00에 자동 업데이트 수행한다.

• **SMU_Students.txt 파일 형식**: `Name\tStudentID\tDepartment\tGrade\tGPA\tEmail\tPhone\n`
  - 탭 구분자로 필드 분리
  - UTF-8 인코딩

• **파일 관리 함수**
  - `load_students_from_file()`: 서버 시작 시 파일에서 학생 정보 로드 → 연결리스트 생성
  - `save_students_to_file()`: 메모리의 학생 정보를 파일에 저장
  - `scheduled_update()`: 매일 01:00 자동 업데이트

##### ▪ 요청 라우팅 (request_router.c/h)

클라이언트 요청의 패킷 타입을 분석하고 해당 핸들러 함수로 라우팅한다.

```c
/* 요청 타입별 핸들러 함수 테이블 */
typedef struct {
    const char *type;
    int (*handler)(ClientSession *session, const char *fields[]);
} RequestHandler;

/* 라우팅 함수 */
int route_request(ClientSession *session, const char *packet);
```

##### ▪ 브로드캐스트 (broadcast.c/h)

특정 사용자에게 데이터를 전송하거나, 모든 연결된 클라이언트에게 알림을 전송한다.

• `send_to_client(socket_fd, packet)`: 특정 클라이언트에게 패킷 전송
• `broadcast_notification(message)`: 모든 접속 클라이언트에게 알림 전송 (선택)

#### □ 클라이언트 (client.c, menu.c, etc.)

##### ▪ 소켓 통신 모듈 (net.c/h)

서버와의 TCP 소켓 통신을 담당한다.

• **클라이언트 상태 구조체** (state.h)
```c
typedef struct {
    int   sock_fd;              /* 서버 소켓 FD */
    char  user_id[21];          /* 로그인한 사용자 ID */
    char  user_name[51];        /* 사용자 이름 */
    char  user_dept[51];        /* 사용자 부서 */
    int   login_status;         /* 0=미인증, 1=인증됨 */
} ClientState;
```

• **통신 함수**
  - `connect_to_server(host, port)`: 서버 연결
  - `send_packet(packet)`: 패킷 전송
  - `recv_packet()`: 서버에서 패킷 수신
  - `disconnect_from_server()`: 연결 종료

##### ▪ 메뉴 인터페이스 (menu.c/h)

사용자 입력을 받아 메뉴를 표시하고 명령을 처리한다.

• **주요 메뉴**
```
=== 대학 교직원을 위한 학생관리 시스템 ===
1. 로그인 / 회원가입
2. 학생 정보 검색
3. 학생 정보 추가
4. 학생 정보 수정
5. 학생 정보 삭제
6. 학과별 학생 조회
7. 로그아웃
8. 종료
```

• **메뉴 선택에 따른 처리**
  - 각 메뉴별로 사용자로부터 필요한 정보 입력받음
  - 입력 값 검증
  - 서버에 해당 요청 패킷 전송
  - 응답 받아 사용자에게 표시

---

### 2-2. 프로젝트 추진 계획

#### 팀원 역할 분담

| 번호 | 이름 | 역할 | 담당 모듈 |
|------|------|------|---------|
| 1 | (작성) | 팀장 / 서버 개발 | server.c, auth.c, student_list.c |
| 2 | (작성) | 클라이언트 개발 | client.c, menu.c, net.c |
| 3 | (작성) | 통신 프로토콜 / 파일 관리 | protocol.c, file_manager.c, types.h |

#### 프로젝트 추진 상세 일정

수행기간: 2026.04.13(월) ~ 2026.06.30(수), 총 79일

| 작업 항목 | 1주 | 2주 | 3주 | 4주 | 5주 | 6주 | 7주 | 8주 | 9주 | 10주 | 11주 | 12주 |
|---------|-----|-----|-----|-----|-----|-----|-----|-----|-----|------|------|------|
| 프로젝트 기획 및 요구사항 분석 | ● | ● |   |   |   |   |   |   |   |   |   |   |
| [F0] Makefile·공통 모듈·데이터 타입 정의 |   | ● | ● |   |   |   |   |   |   |   |   |   |
| [F0] 프로토콜 정의 및 패킷 생성 함수 |   | ● | ● |   |   |   |   |   |   |   |   |   |
| [F0] 서버 기반(main·client_handler·request_router) |   |   | ● | ● |   |   |   |   |   |   |   |   |
| [F0] 클라이언트 기반(socket·연결) |   |   | ● | ● |   |   |   |   |   |   |   |   |
| [P0] 서버 사용자 인증 모듈(로그인·회원가입) |   |   |   | ● | ● |   |   |   |   |   |   |   |
| [P0] 서버 파일 관리 및 학생 정보 로드 |   |   |   | ● | ● |   |   |   |   |   |   |   |
| [P0] 서버 학생 정보 검색·조회 기능 |   |   |   |   | ● | ● |   |   |   |   |   |   |
| [P0] 클라이언트 로그인·회원가입 화면 |   |   |   |   | ● | ● |   |   |   |   |   |   |
| [P0] 클라이언트 메뉴 및 기본 인터페이스 |   |   |   |   |   | ● | ● |   |   |   |   |   |
| [P1] 서버 학생 정보 추가·삭제·수정 기능 |   |   |   |   |   |   | ● | ● |   |   |   |   |
| [P1] 클라이언트 학생 검색·조회 화면 |   |   |   |   |   |   | ● | ● |   |   |   |   |
| [P1] 클라이언트 학생 정보 추가·수정·삭제 화면 |   |   |   |   |   |   |   | ● | ● |   |   |   |
| [P2] 학과별 학생 조회 기능 |   |   |   |   |   |   |   |   | ● | ● |   |   |
| [P2] 자동 업데이트 스케줄링 (01:00) |   |   |   |   |   |   |   |   | ● | ● |   |   |
| [P2] 추가 검색 필터 및 통계 기능 |   |   |   |   |   |   |   |   |   | ● | ● |   |
| 통합 테스트 및 디버깅 |   |   |   |   |   |   |   |   |   |   | ● | ● |
| 최종 검토 및 제출 |   |   |   |   |   |   |   |   |   |   |   | ● |

**비고**: 일정 표의 ●는 해당 기간 동안 수행되는 작업을 나타냄

---

## References

1. **C 언어 소켓 프로그래밍**
   - POSIX Socket API 공식 문서
   - Beej's Guide to Network Programming (https://beej.us/guide/bgnet/)

2. **C 언어 기초 및 자료구조**
   - 연결리스트 구현 및 활용
   - 동적 메모리 관리 (malloc, free)

3. **파일 I/O 및 문자열 처리**
   - POSIX File I/O (fopen, fread, fwrite, etc.)
   - 탭 구분자 기반 파일 파싱

4. **멀티 프로세스/멀티 스레드 프로그래밍**
   - fork(), wait() 기반 다중 클라이언트 처리
   - 또는 스레드 기반 동시 접속 처리 (pthread)

5. **Windows 콘솔 프로그래밍**
   - Windows API 기반 소켓 (Winsock2)
   - 콘솔 입출력 및 문자 인코딩

---

## 추가 개발 고려사항

### 보안 사항

• 사용자 암호는 평문으로 저장하지 않고 암호화 또는 해싱 고려
• 사용자 입력값 검증 및 SQL Injection / Buffer Overflow 방지 (예: strcpy 대신 strncpy 사용)
• 파일 접근 권한 제어

### 성능 최적화

• 연결리스트의 검색 성능 개선 (대규모 데이터 시 정렬 또는 해시테이블 고려)
• 서버의 동시 클라이언트 처리 최적화
• 메모리 누수 방지 (valgrind 등으로 검증)

### 확장성

• 향후 데이터베이스(SQLite, MySQL) 연동 시 모듈화된 구조 유지
• 그래픽 UI(GTK, Qt) 기반 클라이언트로 업그레이드 가능
• 네트워크 보안 강화 (SSL/TLS 암호화)
