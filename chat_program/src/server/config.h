#pragma once

/* server/config.h
 * protocol.h에 이미 DEFAULT_PORT, MAX_*, FILE_* 상수가 모두 정의되어 있으므로
 * 이 파일은 서버 전용 추가 상수 및 공통 헤더의 일괄 포함 역할만 한다. */

#include "../common/protocol.h"
#include "../common/types.h"
